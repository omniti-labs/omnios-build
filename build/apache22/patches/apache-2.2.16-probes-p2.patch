diff -ruN httpd-2.2.16.orig/configure httpd-2.2.16/configure
--- httpd-2.2.16.orig/configure	2010-07-22 09:07:29.000000000 +0000
+++ httpd-2.2.16/configure	2011-02-23 18:38:20.248658436 +0000
@@ -772,6 +772,7 @@
 PILDFLAGS
 PICFLAGS
 CRYPT_LIBS
+DTRACE
 MKDEP
 EGREP
 GREP
@@ -883,6 +884,7 @@
 with_pcre
 with_port
 with_sslport
+enable_dtrace
 enable_v4_mapped
 enable_exception_hook
 enable_maintainer_mode
@@ -1621,6 +1623,7 @@
   --disable-FEATURE       do not include FEATURE (same as --enable-FEATURE=no)
   --enable-FEATURE[=ARG]  include FEATURE [ARG=yes]
   --enable-layout=LAYOUT
+  --enable-dtrace         Enable DTrace probes
   --enable-v4-mapped      Allow IPv6 sockets to handle IPv4 connections
   --enable-exception-hook Enable fatal exception hook
   --enable-maintainer-mode
@@ -6838,6 +6841,7 @@
 
 fi
 
+
 for ac_header in \
 string.h \
 limits.h \
@@ -6848,7 +6852,8 @@
 strings.h \
 sys/prctl.h \
 sys/processor.h \
-sys/sem.h
+sys/sem.h \
+sys/sdt.h
 
 do :
   as_ac_Header=`$as_echo "ac_cv_header_$ac_header" | $as_tr_sh`
@@ -7175,6 +7180,54 @@
 fi
 
 
+# Check whether --enable-dtrace was given.
+if test "${enable_dtrace+set}" = set; then
+  enableval=$enable_dtrace;
+  enable_dtrace=$enableval
+
+  if test "x$CPPFLAGS" = "x"; then
+    test "x$silent" != "xyes" && echo "  setting CPPFLAGS to \"-DAPR_DTRACE_PROVIDER=ap\""
+    CPPFLAGS="-DAPR_DTRACE_PROVIDER=ap"
+  else
+    apr_addto_bugger="-DAPR_DTRACE_PROVIDER=ap"
+    for i in $apr_addto_bugger; do
+      apr_addto_duplicate="0"
+      for j in $CPPFLAGS; do
+        if test "x$i" = "x$j"; then
+          apr_addto_duplicate="1"
+          break
+        fi
+      done
+      if test $apr_addto_duplicate = "0"; then
+        test "x$silent" != "xyes" && echo "  adding \"$i\" to CPPFLAGS"
+        CPPFLAGS="$CPPFLAGS $i"
+      fi
+    done
+  fi
+
+
+else
+
+  enable_dtrace=no
+
+fi
+
+
+DTRACE=true
+if test $enable_dtrace = "yes" -a "$ac_cv_header_sys_sdt_h" = "yes"; then
+
+cat >>confdefs.h <<\_ACEOF
+#define AP_ENABLE_DTRACE 1
+_ACEOF
+
+    DTRACE="/usr/sbin/dtrace $DTRACEFLAGS"
+    test -f include/apache_probes.h || $DTRACE -h -s apache_probes.d -o include/apache_probes.h
+fi
+
+  APACHE_VAR_SUBST="$APACHE_VAR_SUBST DTRACE"
+
+
+
 
 apr_old_cppflags=$CPPFLAGS
 CPPFLAGS="$CPPFLAGS $INCLUDES"
@@ -7308,6 +7361,9 @@
 
 
 prefix="$orig_prefix"
+cat >static_modules.mk<<EOF
+MODULE_OBJS = \\
+EOF
 
   module_selection=default
   module_default=yes
@@ -7421,23 +7477,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_authn_file.libtool.hack<<EOF
+# probes_authn_file.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_authn_file.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_authn_file.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_authn_file.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_authn_file.d"; then
+        echo "Found probes for authn_file $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_authn_file.d $realobjects && cp probes_authn_file.o .libs/probes_authn_file.o && cp probes_authn_file.libtool.hack probes_authn_file.lo"
+        DTRACE_OBJ=probes_authn_file.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_authn_file.h -s $modpath_current/probes_authn_file.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_authn_file.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_authn_file.o .libs/probes_authn_file.o && cp probes_authn_file.libtool.hack probes_authn_file.lo"
+        DTRACE_OBJ=probes_authn_file.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_AUTHN_FILE_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_authn_file.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_authn_file.d"; then
+        echo "Found probes for authn_file $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_authn_file.d $realobjects && cp probes_authn_file.o .libs/probes_authn_file.o && cp probes_authn_file.libtool.hack probes_authn_file.lo"
+        DTRACE_OBJ=probes_authn_file.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_authn_file.h -s $modpath_current/probes_authn_file.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_authn_file.so $realobjects .libs/probes_authn_file.o \$(MOD_AUTHN_FILE_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_authn_file.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_authn_file.o .libs/probes_authn_file.o && cp probes_authn_file.libtool.hack probes_authn_file.lo"
+        DTRACE_OBJ=probes_authn_file.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_authn_file.so $realobjects .libs/probes_authn_file.o \$(MOD_AUTHN_FILE_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_AUTHN_FILE_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_AUTHN_FILE_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -7514,23 +7610,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_authn_dbm.libtool.hack<<EOF
+# probes_authn_dbm.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_authn_dbm.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_authn_dbm.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_authn_dbm.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_authn_dbm.d"; then
+        echo "Found probes for authn_dbm $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_authn_dbm.d $realobjects && cp probes_authn_dbm.o .libs/probes_authn_dbm.o && cp probes_authn_dbm.libtool.hack probes_authn_dbm.lo"
+        DTRACE_OBJ=probes_authn_dbm.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_authn_dbm.h -s $modpath_current/probes_authn_dbm.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_authn_dbm.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_authn_dbm.o .libs/probes_authn_dbm.o && cp probes_authn_dbm.libtool.hack probes_authn_dbm.lo"
+        DTRACE_OBJ=probes_authn_dbm.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_AUTHN_DBM_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_authn_dbm.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_authn_dbm.d"; then
+        echo "Found probes for authn_dbm $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_authn_dbm.d $realobjects && cp probes_authn_dbm.o .libs/probes_authn_dbm.o && cp probes_authn_dbm.libtool.hack probes_authn_dbm.lo"
+        DTRACE_OBJ=probes_authn_dbm.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_authn_dbm.h -s $modpath_current/probes_authn_dbm.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_authn_dbm.so $realobjects .libs/probes_authn_dbm.o \$(MOD_AUTHN_DBM_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_authn_dbm.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_authn_dbm.o .libs/probes_authn_dbm.o && cp probes_authn_dbm.libtool.hack probes_authn_dbm.lo"
+        DTRACE_OBJ=probes_authn_dbm.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_authn_dbm.so $realobjects .libs/probes_authn_dbm.o \$(MOD_AUTHN_DBM_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_AUTHN_DBM_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_AUTHN_DBM_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -7607,23 +7743,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_authn_anon.libtool.hack<<EOF
+# probes_authn_anon.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_authn_anon.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_authn_anon.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_authn_anon.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_authn_anon.d"; then
+        echo "Found probes for authn_anon $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_authn_anon.d $realobjects && cp probes_authn_anon.o .libs/probes_authn_anon.o && cp probes_authn_anon.libtool.hack probes_authn_anon.lo"
+        DTRACE_OBJ=probes_authn_anon.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_authn_anon.h -s $modpath_current/probes_authn_anon.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_authn_anon.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_authn_anon.o .libs/probes_authn_anon.o && cp probes_authn_anon.libtool.hack probes_authn_anon.lo"
+        DTRACE_OBJ=probes_authn_anon.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_AUTHN_ANON_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_authn_anon.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_authn_anon.d"; then
+        echo "Found probes for authn_anon $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_authn_anon.d $realobjects && cp probes_authn_anon.o .libs/probes_authn_anon.o && cp probes_authn_anon.libtool.hack probes_authn_anon.lo"
+        DTRACE_OBJ=probes_authn_anon.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_authn_anon.h -s $modpath_current/probes_authn_anon.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_authn_anon.so $realobjects .libs/probes_authn_anon.o \$(MOD_AUTHN_ANON_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_authn_anon.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_authn_anon.o .libs/probes_authn_anon.o && cp probes_authn_anon.libtool.hack probes_authn_anon.lo"
+        DTRACE_OBJ=probes_authn_anon.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_authn_anon.so $realobjects .libs/probes_authn_anon.o \$(MOD_AUTHN_ANON_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_AUTHN_ANON_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_AUTHN_ANON_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -7700,23 +7876,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_authn_dbd.libtool.hack<<EOF
+# probes_authn_dbd.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_authn_dbd.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_authn_dbd.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_authn_dbd.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_authn_dbd.d"; then
+        echo "Found probes for authn_dbd $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_authn_dbd.d $realobjects && cp probes_authn_dbd.o .libs/probes_authn_dbd.o && cp probes_authn_dbd.libtool.hack probes_authn_dbd.lo"
+        DTRACE_OBJ=probes_authn_dbd.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_authn_dbd.h -s $modpath_current/probes_authn_dbd.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_authn_dbd.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_authn_dbd.o .libs/probes_authn_dbd.o && cp probes_authn_dbd.libtool.hack probes_authn_dbd.lo"
+        DTRACE_OBJ=probes_authn_dbd.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_AUTHN_DBD_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_authn_dbd.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_authn_dbd.d"; then
+        echo "Found probes for authn_dbd $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_authn_dbd.d $realobjects && cp probes_authn_dbd.o .libs/probes_authn_dbd.o && cp probes_authn_dbd.libtool.hack probes_authn_dbd.lo"
+        DTRACE_OBJ=probes_authn_dbd.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_authn_dbd.h -s $modpath_current/probes_authn_dbd.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_authn_dbd.so $realobjects .libs/probes_authn_dbd.o \$(MOD_AUTHN_DBD_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_authn_dbd.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_authn_dbd.o .libs/probes_authn_dbd.o && cp probes_authn_dbd.libtool.hack probes_authn_dbd.lo"
+        DTRACE_OBJ=probes_authn_dbd.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_authn_dbd.so $realobjects .libs/probes_authn_dbd.o \$(MOD_AUTHN_DBD_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_AUTHN_DBD_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_AUTHN_DBD_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -7794,23 +8010,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_authn_default.libtool.hack<<EOF
+# probes_authn_default.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_authn_default.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_authn_default.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_authn_default.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_authn_default.d"; then
+        echo "Found probes for authn_default $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_authn_default.d $realobjects && cp probes_authn_default.o .libs/probes_authn_default.o && cp probes_authn_default.libtool.hack probes_authn_default.lo"
+        DTRACE_OBJ=probes_authn_default.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_authn_default.h -s $modpath_current/probes_authn_default.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_authn_default.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_authn_default.o .libs/probes_authn_default.o && cp probes_authn_default.libtool.hack probes_authn_default.lo"
+        DTRACE_OBJ=probes_authn_default.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_AUTHN_DEFAULT_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_authn_default.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_authn_default.d"; then
+        echo "Found probes for authn_default $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_authn_default.d $realobjects && cp probes_authn_default.o .libs/probes_authn_default.o && cp probes_authn_default.libtool.hack probes_authn_default.lo"
+        DTRACE_OBJ=probes_authn_default.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_authn_default.h -s $modpath_current/probes_authn_default.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_authn_default.so $realobjects .libs/probes_authn_default.o \$(MOD_AUTHN_DEFAULT_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_authn_default.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_authn_default.o .libs/probes_authn_default.o && cp probes_authn_default.libtool.hack probes_authn_default.lo"
+        DTRACE_OBJ=probes_authn_default.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_authn_default.so $realobjects .libs/probes_authn_default.o \$(MOD_AUTHN_DEFAULT_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_AUTHN_DEFAULT_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_AUTHN_DEFAULT_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -7888,23 +8144,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_authn_alias.libtool.hack<<EOF
+# probes_authn_alias.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_authn_alias.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_authn_alias.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_authn_alias.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_authn_alias.d"; then
+        echo "Found probes for authn_alias $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_authn_alias.d $realobjects && cp probes_authn_alias.o .libs/probes_authn_alias.o && cp probes_authn_alias.libtool.hack probes_authn_alias.lo"
+        DTRACE_OBJ=probes_authn_alias.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_authn_alias.h -s $modpath_current/probes_authn_alias.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_authn_alias.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_authn_alias.o .libs/probes_authn_alias.o && cp probes_authn_alias.libtool.hack probes_authn_alias.lo"
+        DTRACE_OBJ=probes_authn_alias.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_AUTHN_ALIAS_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_authn_alias.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_authn_alias.d"; then
+        echo "Found probes for authn_alias $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_authn_alias.d $realobjects && cp probes_authn_alias.o .libs/probes_authn_alias.o && cp probes_authn_alias.libtool.hack probes_authn_alias.lo"
+        DTRACE_OBJ=probes_authn_alias.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_authn_alias.h -s $modpath_current/probes_authn_alias.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_authn_alias.so $realobjects .libs/probes_authn_alias.o \$(MOD_AUTHN_ALIAS_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_authn_alias.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_authn_alias.o .libs/probes_authn_alias.o && cp probes_authn_alias.libtool.hack probes_authn_alias.lo"
+        DTRACE_OBJ=probes_authn_alias.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_authn_alias.so $realobjects .libs/probes_authn_alias.o \$(MOD_AUTHN_ALIAS_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_AUTHN_ALIAS_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_AUTHN_ALIAS_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -7982,23 +8278,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_authz_host.libtool.hack<<EOF
+# probes_authz_host.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_authz_host.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_authz_host.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_authz_host.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_authz_host.d"; then
+        echo "Found probes for authz_host $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_authz_host.d $realobjects && cp probes_authz_host.o .libs/probes_authz_host.o && cp probes_authz_host.libtool.hack probes_authz_host.lo"
+        DTRACE_OBJ=probes_authz_host.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_authz_host.h -s $modpath_current/probes_authz_host.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_authz_host.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_authz_host.o .libs/probes_authz_host.o && cp probes_authz_host.libtool.hack probes_authz_host.lo"
+        DTRACE_OBJ=probes_authz_host.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_AUTHZ_HOST_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_authz_host.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_authz_host.d"; then
+        echo "Found probes for authz_host $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_authz_host.d $realobjects && cp probes_authz_host.o .libs/probes_authz_host.o && cp probes_authz_host.libtool.hack probes_authz_host.lo"
+        DTRACE_OBJ=probes_authz_host.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_authz_host.h -s $modpath_current/probes_authz_host.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_authz_host.so $realobjects .libs/probes_authz_host.o \$(MOD_AUTHZ_HOST_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_authz_host.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_authz_host.o .libs/probes_authz_host.o && cp probes_authz_host.libtool.hack probes_authz_host.lo"
+        DTRACE_OBJ=probes_authz_host.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_authz_host.so $realobjects .libs/probes_authz_host.o \$(MOD_AUTHZ_HOST_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_AUTHZ_HOST_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_AUTHZ_HOST_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -8075,23 +8411,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_authz_groupfile.libtool.hack<<EOF
+# probes_authz_groupfile.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_authz_groupfile.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_authz_groupfile.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_authz_groupfile.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_authz_groupfile.d"; then
+        echo "Found probes for authz_groupfile $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_authz_groupfile.d $realobjects && cp probes_authz_groupfile.o .libs/probes_authz_groupfile.o && cp probes_authz_groupfile.libtool.hack probes_authz_groupfile.lo"
+        DTRACE_OBJ=probes_authz_groupfile.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_authz_groupfile.h -s $modpath_current/probes_authz_groupfile.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_authz_groupfile.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_authz_groupfile.o .libs/probes_authz_groupfile.o && cp probes_authz_groupfile.libtool.hack probes_authz_groupfile.lo"
+        DTRACE_OBJ=probes_authz_groupfile.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_AUTHZ_GROUPFILE_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_authz_groupfile.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_authz_groupfile.d"; then
+        echo "Found probes for authz_groupfile $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_authz_groupfile.d $realobjects && cp probes_authz_groupfile.o .libs/probes_authz_groupfile.o && cp probes_authz_groupfile.libtool.hack probes_authz_groupfile.lo"
+        DTRACE_OBJ=probes_authz_groupfile.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_authz_groupfile.h -s $modpath_current/probes_authz_groupfile.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_authz_groupfile.so $realobjects .libs/probes_authz_groupfile.o \$(MOD_AUTHZ_GROUPFILE_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_authz_groupfile.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_authz_groupfile.o .libs/probes_authz_groupfile.o && cp probes_authz_groupfile.libtool.hack probes_authz_groupfile.lo"
+        DTRACE_OBJ=probes_authz_groupfile.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_authz_groupfile.so $realobjects .libs/probes_authz_groupfile.o \$(MOD_AUTHZ_GROUPFILE_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_AUTHZ_GROUPFILE_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_AUTHZ_GROUPFILE_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -8168,23 +8544,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_authz_user.libtool.hack<<EOF
+# probes_authz_user.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_authz_user.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_authz_user.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_authz_user.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_authz_user.d"; then
+        echo "Found probes for authz_user $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_authz_user.d $realobjects && cp probes_authz_user.o .libs/probes_authz_user.o && cp probes_authz_user.libtool.hack probes_authz_user.lo"
+        DTRACE_OBJ=probes_authz_user.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_authz_user.h -s $modpath_current/probes_authz_user.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_authz_user.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_authz_user.o .libs/probes_authz_user.o && cp probes_authz_user.libtool.hack probes_authz_user.lo"
+        DTRACE_OBJ=probes_authz_user.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_AUTHZ_USER_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_authz_user.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_authz_user.d"; then
+        echo "Found probes for authz_user $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_authz_user.d $realobjects && cp probes_authz_user.o .libs/probes_authz_user.o && cp probes_authz_user.libtool.hack probes_authz_user.lo"
+        DTRACE_OBJ=probes_authz_user.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_authz_user.h -s $modpath_current/probes_authz_user.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_authz_user.so $realobjects .libs/probes_authz_user.o \$(MOD_AUTHZ_USER_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_authz_user.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_authz_user.o .libs/probes_authz_user.o && cp probes_authz_user.libtool.hack probes_authz_user.lo"
+        DTRACE_OBJ=probes_authz_user.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_authz_user.so $realobjects .libs/probes_authz_user.o \$(MOD_AUTHZ_USER_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_AUTHZ_USER_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_AUTHZ_USER_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -8261,23 +8677,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_authz_dbm.libtool.hack<<EOF
+# probes_authz_dbm.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_authz_dbm.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_authz_dbm.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_authz_dbm.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_authz_dbm.d"; then
+        echo "Found probes for authz_dbm $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_authz_dbm.d $realobjects && cp probes_authz_dbm.o .libs/probes_authz_dbm.o && cp probes_authz_dbm.libtool.hack probes_authz_dbm.lo"
+        DTRACE_OBJ=probes_authz_dbm.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_authz_dbm.h -s $modpath_current/probes_authz_dbm.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_authz_dbm.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_authz_dbm.o .libs/probes_authz_dbm.o && cp probes_authz_dbm.libtool.hack probes_authz_dbm.lo"
+        DTRACE_OBJ=probes_authz_dbm.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_AUTHZ_DBM_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_authz_dbm.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_authz_dbm.d"; then
+        echo "Found probes for authz_dbm $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_authz_dbm.d $realobjects && cp probes_authz_dbm.o .libs/probes_authz_dbm.o && cp probes_authz_dbm.libtool.hack probes_authz_dbm.lo"
+        DTRACE_OBJ=probes_authz_dbm.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_authz_dbm.h -s $modpath_current/probes_authz_dbm.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_authz_dbm.so $realobjects .libs/probes_authz_dbm.o \$(MOD_AUTHZ_DBM_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_authz_dbm.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_authz_dbm.o .libs/probes_authz_dbm.o && cp probes_authz_dbm.libtool.hack probes_authz_dbm.lo"
+        DTRACE_OBJ=probes_authz_dbm.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_authz_dbm.so $realobjects .libs/probes_authz_dbm.o \$(MOD_AUTHZ_DBM_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_AUTHZ_DBM_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_AUTHZ_DBM_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -8354,23 +8810,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_authz_owner.libtool.hack<<EOF
+# probes_authz_owner.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_authz_owner.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_authz_owner.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_authz_owner.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_authz_owner.d"; then
+        echo "Found probes for authz_owner $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_authz_owner.d $realobjects && cp probes_authz_owner.o .libs/probes_authz_owner.o && cp probes_authz_owner.libtool.hack probes_authz_owner.lo"
+        DTRACE_OBJ=probes_authz_owner.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_authz_owner.h -s $modpath_current/probes_authz_owner.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_authz_owner.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_authz_owner.o .libs/probes_authz_owner.o && cp probes_authz_owner.libtool.hack probes_authz_owner.lo"
+        DTRACE_OBJ=probes_authz_owner.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_AUTHZ_OWNER_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_authz_owner.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_authz_owner.d"; then
+        echo "Found probes for authz_owner $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_authz_owner.d $realobjects && cp probes_authz_owner.o .libs/probes_authz_owner.o && cp probes_authz_owner.libtool.hack probes_authz_owner.lo"
+        DTRACE_OBJ=probes_authz_owner.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_authz_owner.h -s $modpath_current/probes_authz_owner.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_authz_owner.so $realobjects .libs/probes_authz_owner.o \$(MOD_AUTHZ_OWNER_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_authz_owner.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_authz_owner.o .libs/probes_authz_owner.o && cp probes_authz_owner.libtool.hack probes_authz_owner.lo"
+        DTRACE_OBJ=probes_authz_owner.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_authz_owner.so $realobjects .libs/probes_authz_owner.o \$(MOD_AUTHZ_OWNER_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_AUTHZ_OWNER_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_AUTHZ_OWNER_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -8462,23 +8958,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_authnz_ldap.libtool.hack<<EOF
+# probes_authnz_ldap.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_authnz_ldap.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_authnz_ldap.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_authnz_ldap.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_authnz_ldap.d"; then
+        echo "Found probes for authnz_ldap $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_authnz_ldap.d $realobjects && cp probes_authnz_ldap.o .libs/probes_authnz_ldap.o && cp probes_authnz_ldap.libtool.hack probes_authnz_ldap.lo"
+        DTRACE_OBJ=probes_authnz_ldap.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_authnz_ldap.h -s $modpath_current/probes_authnz_ldap.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_authnz_ldap.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_authnz_ldap.o .libs/probes_authnz_ldap.o && cp probes_authnz_ldap.libtool.hack probes_authnz_ldap.lo"
+        DTRACE_OBJ=probes_authnz_ldap.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_AUTHNZ_LDAP_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_authnz_ldap.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_authnz_ldap.d"; then
+        echo "Found probes for authnz_ldap $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_authnz_ldap.d $realobjects && cp probes_authnz_ldap.o .libs/probes_authnz_ldap.o && cp probes_authnz_ldap.libtool.hack probes_authnz_ldap.lo"
+        DTRACE_OBJ=probes_authnz_ldap.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_authnz_ldap.h -s $modpath_current/probes_authnz_ldap.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_authnz_ldap.so $realobjects .libs/probes_authnz_ldap.o \$(MOD_AUTHNZ_LDAP_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_authnz_ldap.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_authnz_ldap.o .libs/probes_authnz_ldap.o && cp probes_authnz_ldap.libtool.hack probes_authnz_ldap.lo"
+        DTRACE_OBJ=probes_authnz_ldap.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_authnz_ldap.so $realobjects .libs/probes_authnz_ldap.o \$(MOD_AUTHNZ_LDAP_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_AUTHNZ_LDAP_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_AUTHNZ_LDAP_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -8556,23 +9092,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_authz_default.libtool.hack<<EOF
+# probes_authz_default.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_authz_default.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_authz_default.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_authz_default.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_authz_default.d"; then
+        echo "Found probes for authz_default $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_authz_default.d $realobjects && cp probes_authz_default.o .libs/probes_authz_default.o && cp probes_authz_default.libtool.hack probes_authz_default.lo"
+        DTRACE_OBJ=probes_authz_default.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_authz_default.h -s $modpath_current/probes_authz_default.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_authz_default.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_authz_default.o .libs/probes_authz_default.o && cp probes_authz_default.libtool.hack probes_authz_default.lo"
+        DTRACE_OBJ=probes_authz_default.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_AUTHZ_DEFAULT_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_authz_default.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_authz_default.d"; then
+        echo "Found probes for authz_default $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_authz_default.d $realobjects && cp probes_authz_default.o .libs/probes_authz_default.o && cp probes_authz_default.libtool.hack probes_authz_default.lo"
+        DTRACE_OBJ=probes_authz_default.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_authz_default.h -s $modpath_current/probes_authz_default.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_authz_default.so $realobjects .libs/probes_authz_default.o \$(MOD_AUTHZ_DEFAULT_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_authz_default.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_authz_default.o .libs/probes_authz_default.o && cp probes_authz_default.libtool.hack probes_authz_default.lo"
+        DTRACE_OBJ=probes_authz_default.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_authz_default.so $realobjects .libs/probes_authz_default.o \$(MOD_AUTHZ_DEFAULT_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_AUTHZ_DEFAULT_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_AUTHZ_DEFAULT_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -8651,23 +9227,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_auth_basic.libtool.hack<<EOF
+# probes_auth_basic.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_auth_basic.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_auth_basic.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_auth_basic.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_auth_basic.d"; then
+        echo "Found probes for auth_basic $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_auth_basic.d $realobjects && cp probes_auth_basic.o .libs/probes_auth_basic.o && cp probes_auth_basic.libtool.hack probes_auth_basic.lo"
+        DTRACE_OBJ=probes_auth_basic.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_auth_basic.h -s $modpath_current/probes_auth_basic.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_auth_basic.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_auth_basic.o .libs/probes_auth_basic.o && cp probes_auth_basic.libtool.hack probes_auth_basic.lo"
+        DTRACE_OBJ=probes_auth_basic.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_AUTH_BASIC_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_auth_basic.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_auth_basic.d"; then
+        echo "Found probes for auth_basic $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_auth_basic.d $realobjects && cp probes_auth_basic.o .libs/probes_auth_basic.o && cp probes_auth_basic.libtool.hack probes_auth_basic.lo"
+        DTRACE_OBJ=probes_auth_basic.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_auth_basic.h -s $modpath_current/probes_auth_basic.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_auth_basic.so $realobjects .libs/probes_auth_basic.o \$(MOD_AUTH_BASIC_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_auth_basic.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_auth_basic.o .libs/probes_auth_basic.o && cp probes_auth_basic.libtool.hack probes_auth_basic.lo"
+        DTRACE_OBJ=probes_auth_basic.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_auth_basic.so $realobjects .libs/probes_auth_basic.o \$(MOD_AUTH_BASIC_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_AUTH_BASIC_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_AUTH_BASIC_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -8783,23 +9399,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_auth_digest.libtool.hack<<EOF
+# probes_auth_digest.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_auth_digest.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_auth_digest.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_auth_digest.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_auth_digest.d"; then
+        echo "Found probes for auth_digest $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_auth_digest.d $realobjects && cp probes_auth_digest.o .libs/probes_auth_digest.o && cp probes_auth_digest.libtool.hack probes_auth_digest.lo"
+        DTRACE_OBJ=probes_auth_digest.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_auth_digest.h -s $modpath_current/probes_auth_digest.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_auth_digest.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_auth_digest.o .libs/probes_auth_digest.o && cp probes_auth_digest.libtool.hack probes_auth_digest.lo"
+        DTRACE_OBJ=probes_auth_digest.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_AUTH_DIGEST_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_auth_digest.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_auth_digest.d"; then
+        echo "Found probes for auth_digest $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_auth_digest.d $realobjects && cp probes_auth_digest.o .libs/probes_auth_digest.o && cp probes_auth_digest.libtool.hack probes_auth_digest.lo"
+        DTRACE_OBJ=probes_auth_digest.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_auth_digest.h -s $modpath_current/probes_auth_digest.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_auth_digest.so $realobjects .libs/probes_auth_digest.o \$(MOD_AUTH_DIGEST_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_auth_digest.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_auth_digest.o .libs/probes_auth_digest.o && cp probes_auth_digest.libtool.hack probes_auth_digest.lo"
+        DTRACE_OBJ=probes_auth_digest.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_auth_digest.so $realobjects .libs/probes_auth_digest.o \$(MOD_AUTH_DIGEST_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_AUTH_DIGEST_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_AUTH_DIGEST_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -8902,23 +9558,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_isapi.libtool.hack<<EOF
+# probes_isapi.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_isapi.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_isapi.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_isapi.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_isapi.d"; then
+        echo "Found probes for isapi $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_isapi.d $realobjects && cp probes_isapi.o .libs/probes_isapi.o && cp probes_isapi.libtool.hack probes_isapi.lo"
+        DTRACE_OBJ=probes_isapi.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_isapi.h -s $modpath_current/probes_isapi.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_isapi.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_isapi.o .libs/probes_isapi.o && cp probes_isapi.libtool.hack probes_isapi.lo"
+        DTRACE_OBJ=probes_isapi.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_ISAPI_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_isapi.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_isapi.d"; then
+        echo "Found probes for isapi $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_isapi.d $realobjects && cp probes_isapi.o .libs/probes_isapi.o && cp probes_isapi.libtool.hack probes_isapi.lo"
+        DTRACE_OBJ=probes_isapi.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_isapi.h -s $modpath_current/probes_isapi.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_isapi.so $realobjects .libs/probes_isapi.o \$(MOD_ISAPI_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_isapi.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_isapi.o .libs/probes_isapi.o && cp probes_isapi.libtool.hack probes_isapi.lo"
+        DTRACE_OBJ=probes_isapi.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_isapi.so $realobjects .libs/probes_isapi.o \$(MOD_ISAPI_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_ISAPI_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_ISAPI_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -9021,23 +9717,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_file_cache.libtool.hack<<EOF
+# probes_file_cache.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_file_cache.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_file_cache.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_file_cache.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_file_cache.d"; then
+        echo "Found probes for file_cache $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_file_cache.d $realobjects && cp probes_file_cache.o .libs/probes_file_cache.o && cp probes_file_cache.libtool.hack probes_file_cache.lo"
+        DTRACE_OBJ=probes_file_cache.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_file_cache.h -s $modpath_current/probes_file_cache.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_file_cache.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_file_cache.o .libs/probes_file_cache.o && cp probes_file_cache.libtool.hack probes_file_cache.lo"
+        DTRACE_OBJ=probes_file_cache.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_FILE_CACHE_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_file_cache.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_file_cache.d"; then
+        echo "Found probes for file_cache $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_file_cache.d $realobjects && cp probes_file_cache.o .libs/probes_file_cache.o && cp probes_file_cache.libtool.hack probes_file_cache.lo"
+        DTRACE_OBJ=probes_file_cache.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_file_cache.h -s $modpath_current/probes_file_cache.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_file_cache.so $realobjects .libs/probes_file_cache.o \$(MOD_FILE_CACHE_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_file_cache.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_file_cache.o .libs/probes_file_cache.o && cp probes_file_cache.libtool.hack probes_file_cache.lo"
+        DTRACE_OBJ=probes_file_cache.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_file_cache.so $realobjects .libs/probes_file_cache.o \$(MOD_FILE_CACHE_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_FILE_CACHE_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_FILE_CACHE_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -9117,23 +9853,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_cache.libtool.hack<<EOF
+# probes_cache.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_cache.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_cache.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_cache.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_cache.d"; then
+        echo "Found probes for cache $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_cache.d $realobjects && cp probes_cache.o .libs/probes_cache.o && cp probes_cache.libtool.hack probes_cache.lo"
+        DTRACE_OBJ=probes_cache.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_cache.h -s $modpath_current/probes_cache.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_cache.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_cache.o .libs/probes_cache.o && cp probes_cache.libtool.hack probes_cache.lo"
+        DTRACE_OBJ=probes_cache.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_CACHE_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_cache.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_cache.d"; then
+        echo "Found probes for cache $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_cache.d $realobjects && cp probes_cache.o .libs/probes_cache.o && cp probes_cache.libtool.hack probes_cache.lo"
+        DTRACE_OBJ=probes_cache.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_cache.h -s $modpath_current/probes_cache.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_cache.so $realobjects .libs/probes_cache.o \$(MOD_CACHE_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_cache.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_cache.o .libs/probes_cache.o && cp probes_cache.libtool.hack probes_cache.lo"
+        DTRACE_OBJ=probes_cache.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_cache.so $realobjects .libs/probes_cache.o \$(MOD_CACHE_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_CACHE_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_CACHE_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -9210,23 +9986,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_disk_cache.libtool.hack<<EOF
+# probes_disk_cache.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_disk_cache.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_disk_cache.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_disk_cache.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_disk_cache.d"; then
+        echo "Found probes for disk_cache $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_disk_cache.d $realobjects && cp probes_disk_cache.o .libs/probes_disk_cache.o && cp probes_disk_cache.libtool.hack probes_disk_cache.lo"
+        DTRACE_OBJ=probes_disk_cache.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_disk_cache.h -s $modpath_current/probes_disk_cache.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_disk_cache.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_disk_cache.o .libs/probes_disk_cache.o && cp probes_disk_cache.libtool.hack probes_disk_cache.lo"
+        DTRACE_OBJ=probes_disk_cache.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_DISK_CACHE_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_disk_cache.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_disk_cache.d"; then
+        echo "Found probes for disk_cache $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_disk_cache.d $realobjects && cp probes_disk_cache.o .libs/probes_disk_cache.o && cp probes_disk_cache.libtool.hack probes_disk_cache.lo"
+        DTRACE_OBJ=probes_disk_cache.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_disk_cache.h -s $modpath_current/probes_disk_cache.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_disk_cache.so $realobjects .libs/probes_disk_cache.o \$(MOD_DISK_CACHE_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_disk_cache.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_disk_cache.o .libs/probes_disk_cache.o && cp probes_disk_cache.libtool.hack probes_disk_cache.lo"
+        DTRACE_OBJ=probes_disk_cache.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_disk_cache.so $realobjects .libs/probes_disk_cache.o \$(MOD_DISK_CACHE_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_DISK_CACHE_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_DISK_CACHE_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -9303,23 +10119,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_mem_cache.libtool.hack<<EOF
+# probes_mem_cache.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_mem_cache.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_mem_cache.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_mem_cache.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_mem_cache.d"; then
+        echo "Found probes for mem_cache $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_mem_cache.d $realobjects && cp probes_mem_cache.o .libs/probes_mem_cache.o && cp probes_mem_cache.libtool.hack probes_mem_cache.lo"
+        DTRACE_OBJ=probes_mem_cache.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_mem_cache.h -s $modpath_current/probes_mem_cache.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_mem_cache.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_mem_cache.o .libs/probes_mem_cache.o && cp probes_mem_cache.libtool.hack probes_mem_cache.lo"
+        DTRACE_OBJ=probes_mem_cache.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_MEM_CACHE_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_mem_cache.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_mem_cache.d"; then
+        echo "Found probes for mem_cache $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_mem_cache.d $realobjects && cp probes_mem_cache.o .libs/probes_mem_cache.o && cp probes_mem_cache.libtool.hack probes_mem_cache.lo"
+        DTRACE_OBJ=probes_mem_cache.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_mem_cache.h -s $modpath_current/probes_mem_cache.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_mem_cache.so $realobjects .libs/probes_mem_cache.o \$(MOD_MEM_CACHE_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_mem_cache.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_mem_cache.o .libs/probes_mem_cache.o && cp probes_mem_cache.libtool.hack probes_mem_cache.lo"
+        DTRACE_OBJ=probes_mem_cache.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_mem_cache.so $realobjects .libs/probes_mem_cache.o \$(MOD_MEM_CACHE_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_MEM_CACHE_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_MEM_CACHE_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -9421,23 +10277,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_dbd.libtool.hack<<EOF
+# probes_dbd.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_dbd.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_dbd.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_dbd.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_dbd.d"; then
+        echo "Found probes for dbd $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_dbd.d $realobjects && cp probes_dbd.o .libs/probes_dbd.o && cp probes_dbd.libtool.hack probes_dbd.lo"
+        DTRACE_OBJ=probes_dbd.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_dbd.h -s $modpath_current/probes_dbd.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_dbd.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_dbd.o .libs/probes_dbd.o && cp probes_dbd.libtool.hack probes_dbd.lo"
+        DTRACE_OBJ=probes_dbd.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_DBD_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_dbd.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_dbd.d"; then
+        echo "Found probes for dbd $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_dbd.d $realobjects && cp probes_dbd.o .libs/probes_dbd.o && cp probes_dbd.libtool.hack probes_dbd.lo"
+        DTRACE_OBJ=probes_dbd.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_dbd.h -s $modpath_current/probes_dbd.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_dbd.so $realobjects .libs/probes_dbd.o \$(MOD_DBD_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_dbd.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_dbd.o .libs/probes_dbd.o && cp probes_dbd.libtool.hack probes_dbd.lo"
+        DTRACE_OBJ=probes_dbd.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_dbd.so $realobjects .libs/probes_dbd.o \$(MOD_DBD_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_DBD_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_DBD_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -9539,23 +10435,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_bucketeer.libtool.hack<<EOF
+# probes_bucketeer.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_bucketeer.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_bucketeer.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_bucketeer.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_bucketeer.d"; then
+        echo "Found probes for bucketeer $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_bucketeer.d $realobjects && cp probes_bucketeer.o .libs/probes_bucketeer.o && cp probes_bucketeer.libtool.hack probes_bucketeer.lo"
+        DTRACE_OBJ=probes_bucketeer.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_bucketeer.h -s $modpath_current/probes_bucketeer.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_bucketeer.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_bucketeer.o .libs/probes_bucketeer.o && cp probes_bucketeer.libtool.hack probes_bucketeer.lo"
+        DTRACE_OBJ=probes_bucketeer.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_BUCKETEER_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_bucketeer.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_bucketeer.d"; then
+        echo "Found probes for bucketeer $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_bucketeer.d $realobjects && cp probes_bucketeer.o .libs/probes_bucketeer.o && cp probes_bucketeer.libtool.hack probes_bucketeer.lo"
+        DTRACE_OBJ=probes_bucketeer.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_bucketeer.h -s $modpath_current/probes_bucketeer.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_bucketeer.so $realobjects .libs/probes_bucketeer.o \$(MOD_BUCKETEER_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_bucketeer.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_bucketeer.o .libs/probes_bucketeer.o && cp probes_bucketeer.libtool.hack probes_bucketeer.lo"
+        DTRACE_OBJ=probes_bucketeer.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_bucketeer.so $realobjects .libs/probes_bucketeer.o \$(MOD_BUCKETEER_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_BUCKETEER_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_BUCKETEER_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -9632,23 +10568,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_dumpio.libtool.hack<<EOF
+# probes_dumpio.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_dumpio.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_dumpio.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_dumpio.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_dumpio.d"; then
+        echo "Found probes for dumpio $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_dumpio.d $realobjects && cp probes_dumpio.o .libs/probes_dumpio.o && cp probes_dumpio.libtool.hack probes_dumpio.lo"
+        DTRACE_OBJ=probes_dumpio.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_dumpio.h -s $modpath_current/probes_dumpio.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_dumpio.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_dumpio.o .libs/probes_dumpio.o && cp probes_dumpio.libtool.hack probes_dumpio.lo"
+        DTRACE_OBJ=probes_dumpio.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_DUMPIO_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_dumpio.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_dumpio.d"; then
+        echo "Found probes for dumpio $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_dumpio.d $realobjects && cp probes_dumpio.o .libs/probes_dumpio.o && cp probes_dumpio.libtool.hack probes_dumpio.lo"
+        DTRACE_OBJ=probes_dumpio.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_dumpio.h -s $modpath_current/probes_dumpio.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_dumpio.so $realobjects .libs/probes_dumpio.o \$(MOD_DUMPIO_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_dumpio.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_dumpio.o .libs/probes_dumpio.o && cp probes_dumpio.libtool.hack probes_dumpio.lo"
+        DTRACE_OBJ=probes_dumpio.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_dumpio.so $realobjects .libs/probes_dumpio.o \$(MOD_DUMPIO_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_DUMPIO_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_DUMPIO_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -9751,23 +10727,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_echo.libtool.hack<<EOF
+# probes_echo.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_echo.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_echo.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_echo.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_echo.d"; then
+        echo "Found probes for echo $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_echo.d $realobjects && cp probes_echo.o .libs/probes_echo.o && cp probes_echo.libtool.hack probes_echo.lo"
+        DTRACE_OBJ=probes_echo.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_echo.h -s $modpath_current/probes_echo.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_echo.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_echo.o .libs/probes_echo.o && cp probes_echo.libtool.hack probes_echo.lo"
+        DTRACE_OBJ=probes_echo.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_ECHO_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_echo.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_echo.d"; then
+        echo "Found probes for echo $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_echo.d $realobjects && cp probes_echo.o .libs/probes_echo.o && cp probes_echo.libtool.hack probes_echo.lo"
+        DTRACE_OBJ=probes_echo.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_echo.h -s $modpath_current/probes_echo.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_echo.so $realobjects .libs/probes_echo.o \$(MOD_ECHO_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_echo.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_echo.o .libs/probes_echo.o && cp probes_echo.libtool.hack probes_echo.lo"
+        DTRACE_OBJ=probes_echo.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_echo.so $realobjects .libs/probes_echo.o \$(MOD_ECHO_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_ECHO_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_ECHO_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -9869,23 +10885,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_example.libtool.hack<<EOF
+# probes_example.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_example.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_example.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_example.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_example.d"; then
+        echo "Found probes for example $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_example.d $realobjects && cp probes_example.o .libs/probes_example.o && cp probes_example.libtool.hack probes_example.lo"
+        DTRACE_OBJ=probes_example.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_example.h -s $modpath_current/probes_example.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_example.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_example.o .libs/probes_example.o && cp probes_example.libtool.hack probes_example.lo"
+        DTRACE_OBJ=probes_example.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_EXAMPLE_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_example.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_example.d"; then
+        echo "Found probes for example $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_example.d $realobjects && cp probes_example.o .libs/probes_example.o && cp probes_example.libtool.hack probes_example.lo"
+        DTRACE_OBJ=probes_example.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_example.h -s $modpath_current/probes_example.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_example.so $realobjects .libs/probes_example.o \$(MOD_EXAMPLE_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_example.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_example.o .libs/probes_example.o && cp probes_example.libtool.hack probes_example.lo"
+        DTRACE_OBJ=probes_example.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_example.so $realobjects .libs/probes_example.o \$(MOD_EXAMPLE_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_EXAMPLE_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_EXAMPLE_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -9962,23 +11018,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_case_filter.libtool.hack<<EOF
+# probes_case_filter.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_case_filter.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_case_filter.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_case_filter.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_case_filter.d"; then
+        echo "Found probes for case_filter $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_case_filter.d $realobjects && cp probes_case_filter.o .libs/probes_case_filter.o && cp probes_case_filter.libtool.hack probes_case_filter.lo"
+        DTRACE_OBJ=probes_case_filter.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_case_filter.h -s $modpath_current/probes_case_filter.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_case_filter.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_case_filter.o .libs/probes_case_filter.o && cp probes_case_filter.libtool.hack probes_case_filter.lo"
+        DTRACE_OBJ=probes_case_filter.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_CASE_FILTER_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_case_filter.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_case_filter.d"; then
+        echo "Found probes for case_filter $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_case_filter.d $realobjects && cp probes_case_filter.o .libs/probes_case_filter.o && cp probes_case_filter.libtool.hack probes_case_filter.lo"
+        DTRACE_OBJ=probes_case_filter.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_case_filter.h -s $modpath_current/probes_case_filter.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_case_filter.so $realobjects .libs/probes_case_filter.o \$(MOD_CASE_FILTER_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_case_filter.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_case_filter.o .libs/probes_case_filter.o && cp probes_case_filter.libtool.hack probes_case_filter.lo"
+        DTRACE_OBJ=probes_case_filter.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_case_filter.so $realobjects .libs/probes_case_filter.o \$(MOD_CASE_FILTER_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_CASE_FILTER_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_CASE_FILTER_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -10055,23 +11151,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_case_filter_in.libtool.hack<<EOF
+# probes_case_filter_in.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_case_filter_in.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_case_filter_in.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_case_filter_in.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_case_filter_in.d"; then
+        echo "Found probes for case_filter_in $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_case_filter_in.d $realobjects && cp probes_case_filter_in.o .libs/probes_case_filter_in.o && cp probes_case_filter_in.libtool.hack probes_case_filter_in.lo"
+        DTRACE_OBJ=probes_case_filter_in.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_case_filter_in.h -s $modpath_current/probes_case_filter_in.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_case_filter_in.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_case_filter_in.o .libs/probes_case_filter_in.o && cp probes_case_filter_in.libtool.hack probes_case_filter_in.lo"
+        DTRACE_OBJ=probes_case_filter_in.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_CASE_FILTER_IN_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_case_filter_in.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_case_filter_in.d"; then
+        echo "Found probes for case_filter_in $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_case_filter_in.d $realobjects && cp probes_case_filter_in.o .libs/probes_case_filter_in.o && cp probes_case_filter_in.libtool.hack probes_case_filter_in.lo"
+        DTRACE_OBJ=probes_case_filter_in.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_case_filter_in.h -s $modpath_current/probes_case_filter_in.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_case_filter_in.so $realobjects .libs/probes_case_filter_in.o \$(MOD_CASE_FILTER_IN_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_case_filter_in.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_case_filter_in.o .libs/probes_case_filter_in.o && cp probes_case_filter_in.libtool.hack probes_case_filter_in.lo"
+        DTRACE_OBJ=probes_case_filter_in.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_case_filter_in.so $realobjects .libs/probes_case_filter_in.o \$(MOD_CASE_FILTER_IN_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_CASE_FILTER_IN_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_CASE_FILTER_IN_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -10267,23 +11403,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_ext_filter.libtool.hack<<EOF
+# probes_ext_filter.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_ext_filter.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_ext_filter.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_ext_filter.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_ext_filter.d"; then
+        echo "Found probes for ext_filter $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_ext_filter.d $realobjects && cp probes_ext_filter.o .libs/probes_ext_filter.o && cp probes_ext_filter.libtool.hack probes_ext_filter.lo"
+        DTRACE_OBJ=probes_ext_filter.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_ext_filter.h -s $modpath_current/probes_ext_filter.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_ext_filter.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_ext_filter.o .libs/probes_ext_filter.o && cp probes_ext_filter.libtool.hack probes_ext_filter.lo"
+        DTRACE_OBJ=probes_ext_filter.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_EXT_FILTER_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_ext_filter.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_ext_filter.d"; then
+        echo "Found probes for ext_filter $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_ext_filter.d $realobjects && cp probes_ext_filter.o .libs/probes_ext_filter.o && cp probes_ext_filter.libtool.hack probes_ext_filter.lo"
+        DTRACE_OBJ=probes_ext_filter.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_ext_filter.h -s $modpath_current/probes_ext_filter.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_ext_filter.so $realobjects .libs/probes_ext_filter.o \$(MOD_EXT_FILTER_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_ext_filter.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_ext_filter.o .libs/probes_ext_filter.o && cp probes_ext_filter.libtool.hack probes_ext_filter.lo"
+        DTRACE_OBJ=probes_ext_filter.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_ext_filter.so $realobjects .libs/probes_ext_filter.o \$(MOD_EXT_FILTER_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_EXT_FILTER_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_EXT_FILTER_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -10360,23 +11536,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_include.libtool.hack<<EOF
+# probes_include.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_include.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_include.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_include.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_include.d"; then
+        echo "Found probes for include $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_include.d $realobjects && cp probes_include.o .libs/probes_include.o && cp probes_include.libtool.hack probes_include.lo"
+        DTRACE_OBJ=probes_include.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_include.h -s $modpath_current/probes_include.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_include.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_include.o .libs/probes_include.o && cp probes_include.libtool.hack probes_include.lo"
+        DTRACE_OBJ=probes_include.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_INCLUDE_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_include.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_include.d"; then
+        echo "Found probes for include $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_include.d $realobjects && cp probes_include.o .libs/probes_include.o && cp probes_include.libtool.hack probes_include.lo"
+        DTRACE_OBJ=probes_include.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_include.h -s $modpath_current/probes_include.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_include.so $realobjects .libs/probes_include.o \$(MOD_INCLUDE_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_include.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_include.o .libs/probes_include.o && cp probes_include.libtool.hack probes_include.lo"
+        DTRACE_OBJ=probes_include.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_include.so $realobjects .libs/probes_include.o \$(MOD_INCLUDE_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_INCLUDE_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_INCLUDE_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -10453,23 +11669,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_filter.libtool.hack<<EOF
+# probes_filter.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_filter.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_filter.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_filter.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_filter.d"; then
+        echo "Found probes for filter $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_filter.d $realobjects && cp probes_filter.o .libs/probes_filter.o && cp probes_filter.libtool.hack probes_filter.lo"
+        DTRACE_OBJ=probes_filter.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_filter.h -s $modpath_current/probes_filter.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_filter.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_filter.o .libs/probes_filter.o && cp probes_filter.libtool.hack probes_filter.lo"
+        DTRACE_OBJ=probes_filter.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_FILTER_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_filter.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_filter.d"; then
+        echo "Found probes for filter $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_filter.d $realobjects && cp probes_filter.o .libs/probes_filter.o && cp probes_filter.libtool.hack probes_filter.lo"
+        DTRACE_OBJ=probes_filter.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_filter.h -s $modpath_current/probes_filter.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_filter.so $realobjects .libs/probes_filter.o \$(MOD_FILTER_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_filter.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_filter.o .libs/probes_filter.o && cp probes_filter.libtool.hack probes_filter.lo"
+        DTRACE_OBJ=probes_filter.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_filter.so $realobjects .libs/probes_filter.o \$(MOD_FILTER_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_FILTER_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_FILTER_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -10546,23 +11802,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_substitute.libtool.hack<<EOF
+# probes_substitute.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_substitute.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_substitute.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_substitute.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_substitute.d"; then
+        echo "Found probes for substitute $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_substitute.d $realobjects && cp probes_substitute.o .libs/probes_substitute.o && cp probes_substitute.libtool.hack probes_substitute.lo"
+        DTRACE_OBJ=probes_substitute.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_substitute.h -s $modpath_current/probes_substitute.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_substitute.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_substitute.o .libs/probes_substitute.o && cp probes_substitute.libtool.hack probes_substitute.lo"
+        DTRACE_OBJ=probes_substitute.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_SUBSTITUTE_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_substitute.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_substitute.d"; then
+        echo "Found probes for substitute $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_substitute.d $realobjects && cp probes_substitute.o .libs/probes_substitute.o && cp probes_substitute.libtool.hack probes_substitute.lo"
+        DTRACE_OBJ=probes_substitute.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_substitute.h -s $modpath_current/probes_substitute.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_substitute.so $realobjects .libs/probes_substitute.o \$(MOD_SUBSTITUTE_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_substitute.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_substitute.o .libs/probes_substitute.o && cp probes_substitute.libtool.hack probes_substitute.lo"
+        DTRACE_OBJ=probes_substitute.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_substitute.so $realobjects .libs/probes_substitute.o \$(MOD_SUBSTITUTE_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_SUBSTITUTE_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_SUBSTITUTE_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -10643,23 +11939,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_charset_lite.libtool.hack<<EOF
+# probes_charset_lite.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_charset_lite.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_charset_lite.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_charset_lite.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_charset_lite.d"; then
+        echo "Found probes for charset_lite $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_charset_lite.d $realobjects && cp probes_charset_lite.o .libs/probes_charset_lite.o && cp probes_charset_lite.libtool.hack probes_charset_lite.lo"
+        DTRACE_OBJ=probes_charset_lite.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_charset_lite.h -s $modpath_current/probes_charset_lite.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_charset_lite.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_charset_lite.o .libs/probes_charset_lite.o && cp probes_charset_lite.libtool.hack probes_charset_lite.lo"
+        DTRACE_OBJ=probes_charset_lite.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_CHARSET_LITE_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_charset_lite.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_charset_lite.d"; then
+        echo "Found probes for charset_lite $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_charset_lite.d $realobjects && cp probes_charset_lite.o .libs/probes_charset_lite.o && cp probes_charset_lite.libtool.hack probes_charset_lite.lo"
+        DTRACE_OBJ=probes_charset_lite.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_charset_lite.h -s $modpath_current/probes_charset_lite.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_charset_lite.so $realobjects .libs/probes_charset_lite.o \$(MOD_CHARSET_LITE_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_charset_lite.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_charset_lite.o .libs/probes_charset_lite.o && cp probes_charset_lite.libtool.hack probes_charset_lite.lo"
+        DTRACE_OBJ=probes_charset_lite.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_charset_lite.so $realobjects .libs/probes_charset_lite.o \$(MOD_CHARSET_LITE_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_CHARSET_LITE_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_CHARSET_LITE_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -10737,23 +12073,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_charset_lite.libtool.hack<<EOF
+# probes_charset_lite.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_charset_lite.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_charset_lite.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_charset_lite.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_charset_lite.d"; then
+        echo "Found probes for charset_lite $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_charset_lite.d $realobjects && cp probes_charset_lite.o .libs/probes_charset_lite.o && cp probes_charset_lite.libtool.hack probes_charset_lite.lo"
+        DTRACE_OBJ=probes_charset_lite.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_charset_lite.h -s $modpath_current/probes_charset_lite.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_charset_lite.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_charset_lite.o .libs/probes_charset_lite.o && cp probes_charset_lite.libtool.hack probes_charset_lite.lo"
+        DTRACE_OBJ=probes_charset_lite.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_CHARSET_LITE_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_charset_lite.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_charset_lite.d"; then
+        echo "Found probes for charset_lite $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_charset_lite.d $realobjects && cp probes_charset_lite.o .libs/probes_charset_lite.o && cp probes_charset_lite.libtool.hack probes_charset_lite.lo"
+        DTRACE_OBJ=probes_charset_lite.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_charset_lite.h -s $modpath_current/probes_charset_lite.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_charset_lite.so $realobjects .libs/probes_charset_lite.o \$(MOD_CHARSET_LITE_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_charset_lite.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_charset_lite.o .libs/probes_charset_lite.o && cp probes_charset_lite.libtool.hack probes_charset_lite.lo"
+        DTRACE_OBJ=probes_charset_lite.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_charset_lite.so $realobjects .libs/probes_charset_lite.o \$(MOD_CHARSET_LITE_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_CHARSET_LITE_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_CHARSET_LITE_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -11025,23 +12401,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_deflate.libtool.hack<<EOF
+# probes_deflate.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_deflate.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_deflate.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_deflate.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_deflate.d"; then
+        echo "Found probes for deflate $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_deflate.d $realobjects && cp probes_deflate.o .libs/probes_deflate.o && cp probes_deflate.libtool.hack probes_deflate.lo"
+        DTRACE_OBJ=probes_deflate.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_deflate.h -s $modpath_current/probes_deflate.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_deflate.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_deflate.o .libs/probes_deflate.o && cp probes_deflate.libtool.hack probes_deflate.lo"
+        DTRACE_OBJ=probes_deflate.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_DEFLATE_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_deflate.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_deflate.d"; then
+        echo "Found probes for deflate $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_deflate.d $realobjects && cp probes_deflate.o .libs/probes_deflate.o && cp probes_deflate.libtool.hack probes_deflate.lo"
+        DTRACE_OBJ=probes_deflate.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_deflate.h -s $modpath_current/probes_deflate.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_deflate.so $realobjects .libs/probes_deflate.o \$(MOD_DEFLATE_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_deflate.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_deflate.o .libs/probes_deflate.o && cp probes_deflate.libtool.hack probes_deflate.lo"
+        DTRACE_OBJ=probes_deflate.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_deflate.so $realobjects .libs/probes_deflate.o \$(MOD_DEFLATE_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_DEFLATE_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_DEFLATE_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -11159,23 +12575,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_ldap.libtool.hack<<EOF
+# probes_ldap.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_ldap.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_ldap.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_ldap.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_ldap.d"; then
+        echo "Found probes for ldap $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_ldap.d $realobjects && cp probes_ldap.o .libs/probes_ldap.o && cp probes_ldap.libtool.hack probes_ldap.lo"
+        DTRACE_OBJ=probes_ldap.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_ldap.h -s $modpath_current/probes_ldap.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_ldap.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_ldap.o .libs/probes_ldap.o && cp probes_ldap.libtool.hack probes_ldap.lo"
+        DTRACE_OBJ=probes_ldap.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_LDAP_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_ldap.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_ldap.d"; then
+        echo "Found probes for ldap $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_ldap.d $realobjects && cp probes_ldap.o .libs/probes_ldap.o && cp probes_ldap.libtool.hack probes_ldap.lo"
+        DTRACE_OBJ=probes_ldap.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_ldap.h -s $modpath_current/probes_ldap.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_ldap.so $realobjects .libs/probes_ldap.o \$(MOD_LDAP_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_ldap.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_ldap.o .libs/probes_ldap.o && cp probes_ldap.libtool.hack probes_ldap.lo"
+        DTRACE_OBJ=probes_ldap.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_ldap.so $realobjects .libs/probes_ldap.o \$(MOD_LDAP_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_LDAP_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_LDAP_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -11278,23 +12734,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_log_config.libtool.hack<<EOF
+# probes_log_config.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_log_config.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_log_config.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_log_config.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_log_config.d"; then
+        echo "Found probes for log_config $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_log_config.d $realobjects && cp probes_log_config.o .libs/probes_log_config.o && cp probes_log_config.libtool.hack probes_log_config.lo"
+        DTRACE_OBJ=probes_log_config.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_log_config.h -s $modpath_current/probes_log_config.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_log_config.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_log_config.o .libs/probes_log_config.o && cp probes_log_config.libtool.hack probes_log_config.lo"
+        DTRACE_OBJ=probes_log_config.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_LOG_CONFIG_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_log_config.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_log_config.d"; then
+        echo "Found probes for log_config $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_log_config.d $realobjects && cp probes_log_config.o .libs/probes_log_config.o && cp probes_log_config.libtool.hack probes_log_config.lo"
+        DTRACE_OBJ=probes_log_config.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_log_config.h -s $modpath_current/probes_log_config.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_log_config.so $realobjects .libs/probes_log_config.o \$(MOD_LOG_CONFIG_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_log_config.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_log_config.o .libs/probes_log_config.o && cp probes_log_config.libtool.hack probes_log_config.lo"
+        DTRACE_OBJ=probes_log_config.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_log_config.so $realobjects .libs/probes_log_config.o \$(MOD_LOG_CONFIG_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_LOG_CONFIG_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_LOG_CONFIG_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -11371,23 +12867,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_log_forensic.libtool.hack<<EOF
+# probes_log_forensic.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_log_forensic.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_log_forensic.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_log_forensic.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_log_forensic.d"; then
+        echo "Found probes for log_forensic $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_log_forensic.d $realobjects && cp probes_log_forensic.o .libs/probes_log_forensic.o && cp probes_log_forensic.libtool.hack probes_log_forensic.lo"
+        DTRACE_OBJ=probes_log_forensic.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_log_forensic.h -s $modpath_current/probes_log_forensic.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_log_forensic.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_log_forensic.o .libs/probes_log_forensic.o && cp probes_log_forensic.libtool.hack probes_log_forensic.lo"
+        DTRACE_OBJ=probes_log_forensic.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_LOG_FORENSIC_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_log_forensic.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_log_forensic.d"; then
+        echo "Found probes for log_forensic $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_log_forensic.d $realobjects && cp probes_log_forensic.o .libs/probes_log_forensic.o && cp probes_log_forensic.libtool.hack probes_log_forensic.lo"
+        DTRACE_OBJ=probes_log_forensic.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_log_forensic.h -s $modpath_current/probes_log_forensic.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_log_forensic.so $realobjects .libs/probes_log_forensic.o \$(MOD_LOG_FORENSIC_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_log_forensic.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_log_forensic.o .libs/probes_log_forensic.o && cp probes_log_forensic.libtool.hack probes_log_forensic.lo"
+        DTRACE_OBJ=probes_log_forensic.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_log_forensic.so $realobjects .libs/probes_log_forensic.o \$(MOD_LOG_FORENSIC_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_LOG_FORENSIC_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_LOG_FORENSIC_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -11490,23 +13026,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_logio.libtool.hack<<EOF
+# probes_logio.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_logio.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_logio.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_logio.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_logio.d"; then
+        echo "Found probes for logio $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_logio.d $realobjects && cp probes_logio.o .libs/probes_logio.o && cp probes_logio.libtool.hack probes_logio.lo"
+        DTRACE_OBJ=probes_logio.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_logio.h -s $modpath_current/probes_logio.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_logio.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_logio.o .libs/probes_logio.o && cp probes_logio.libtool.hack probes_logio.lo"
+        DTRACE_OBJ=probes_logio.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_LOGIO_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_logio.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_logio.d"; then
+        echo "Found probes for logio $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_logio.d $realobjects && cp probes_logio.o .libs/probes_logio.o && cp probes_logio.libtool.hack probes_logio.lo"
+        DTRACE_OBJ=probes_logio.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_logio.h -s $modpath_current/probes_logio.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_logio.so $realobjects .libs/probes_logio.o \$(MOD_LOGIO_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_logio.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_logio.o .libs/probes_logio.o && cp probes_logio.libtool.hack probes_logio.lo"
+        DTRACE_OBJ=probes_logio.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_logio.so $realobjects .libs/probes_logio.o \$(MOD_LOGIO_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_LOGIO_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_LOGIO_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -11609,23 +13185,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_env.libtool.hack<<EOF
+# probes_env.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_env.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_env.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_env.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_env.d"; then
+        echo "Found probes for env $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_env.d $realobjects && cp probes_env.o .libs/probes_env.o && cp probes_env.libtool.hack probes_env.lo"
+        DTRACE_OBJ=probes_env.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_env.h -s $modpath_current/probes_env.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_env.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_env.o .libs/probes_env.o && cp probes_env.libtool.hack probes_env.lo"
+        DTRACE_OBJ=probes_env.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_ENV_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_env.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_env.d"; then
+        echo "Found probes for env $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_env.d $realobjects && cp probes_env.o .libs/probes_env.o && cp probes_env.libtool.hack probes_env.lo"
+        DTRACE_OBJ=probes_env.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_env.h -s $modpath_current/probes_env.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_env.so $realobjects .libs/probes_env.o \$(MOD_ENV_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_env.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_env.o .libs/probes_env.o && cp probes_env.libtool.hack probes_env.lo"
+        DTRACE_OBJ=probes_env.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_env.so $realobjects .libs/probes_env.o \$(MOD_ENV_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_ENV_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_ENV_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -11702,23 +13318,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_mime_magic.libtool.hack<<EOF
+# probes_mime_magic.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_mime_magic.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_mime_magic.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_mime_magic.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_mime_magic.d"; then
+        echo "Found probes for mime_magic $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_mime_magic.d $realobjects && cp probes_mime_magic.o .libs/probes_mime_magic.o && cp probes_mime_magic.libtool.hack probes_mime_magic.lo"
+        DTRACE_OBJ=probes_mime_magic.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_mime_magic.h -s $modpath_current/probes_mime_magic.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_mime_magic.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_mime_magic.o .libs/probes_mime_magic.o && cp probes_mime_magic.libtool.hack probes_mime_magic.lo"
+        DTRACE_OBJ=probes_mime_magic.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_MIME_MAGIC_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_mime_magic.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_mime_magic.d"; then
+        echo "Found probes for mime_magic $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_mime_magic.d $realobjects && cp probes_mime_magic.o .libs/probes_mime_magic.o && cp probes_mime_magic.libtool.hack probes_mime_magic.lo"
+        DTRACE_OBJ=probes_mime_magic.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_mime_magic.h -s $modpath_current/probes_mime_magic.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_mime_magic.so $realobjects .libs/probes_mime_magic.o \$(MOD_MIME_MAGIC_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_mime_magic.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_mime_magic.o .libs/probes_mime_magic.o && cp probes_mime_magic.libtool.hack probes_mime_magic.lo"
+        DTRACE_OBJ=probes_mime_magic.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_mime_magic.so $realobjects .libs/probes_mime_magic.o \$(MOD_MIME_MAGIC_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_MIME_MAGIC_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_MIME_MAGIC_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -11795,23 +13451,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_cern_meta.libtool.hack<<EOF
+# probes_cern_meta.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_cern_meta.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_cern_meta.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_cern_meta.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_cern_meta.d"; then
+        echo "Found probes for cern_meta $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_cern_meta.d $realobjects && cp probes_cern_meta.o .libs/probes_cern_meta.o && cp probes_cern_meta.libtool.hack probes_cern_meta.lo"
+        DTRACE_OBJ=probes_cern_meta.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_cern_meta.h -s $modpath_current/probes_cern_meta.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_cern_meta.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_cern_meta.o .libs/probes_cern_meta.o && cp probes_cern_meta.libtool.hack probes_cern_meta.lo"
+        DTRACE_OBJ=probes_cern_meta.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_CERN_META_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_cern_meta.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_cern_meta.d"; then
+        echo "Found probes for cern_meta $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_cern_meta.d $realobjects && cp probes_cern_meta.o .libs/probes_cern_meta.o && cp probes_cern_meta.libtool.hack probes_cern_meta.lo"
+        DTRACE_OBJ=probes_cern_meta.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_cern_meta.h -s $modpath_current/probes_cern_meta.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_cern_meta.so $realobjects .libs/probes_cern_meta.o \$(MOD_CERN_META_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_cern_meta.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_cern_meta.o .libs/probes_cern_meta.o && cp probes_cern_meta.libtool.hack probes_cern_meta.lo"
+        DTRACE_OBJ=probes_cern_meta.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_cern_meta.so $realobjects .libs/probes_cern_meta.o \$(MOD_CERN_META_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_CERN_META_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_CERN_META_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -11888,23 +13584,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_expires.libtool.hack<<EOF
+# probes_expires.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_expires.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_expires.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_expires.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_expires.d"; then
+        echo "Found probes for expires $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_expires.d $realobjects && cp probes_expires.o .libs/probes_expires.o && cp probes_expires.libtool.hack probes_expires.lo"
+        DTRACE_OBJ=probes_expires.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_expires.h -s $modpath_current/probes_expires.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_expires.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_expires.o .libs/probes_expires.o && cp probes_expires.libtool.hack probes_expires.lo"
+        DTRACE_OBJ=probes_expires.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_EXPIRES_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_expires.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_expires.d"; then
+        echo "Found probes for expires $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_expires.d $realobjects && cp probes_expires.o .libs/probes_expires.o && cp probes_expires.libtool.hack probes_expires.lo"
+        DTRACE_OBJ=probes_expires.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_expires.h -s $modpath_current/probes_expires.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_expires.so $realobjects .libs/probes_expires.o \$(MOD_EXPIRES_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_expires.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_expires.o .libs/probes_expires.o && cp probes_expires.libtool.hack probes_expires.lo"
+        DTRACE_OBJ=probes_expires.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_expires.so $realobjects .libs/probes_expires.o \$(MOD_EXPIRES_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_EXPIRES_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_EXPIRES_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -11981,23 +13717,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_headers.libtool.hack<<EOF
+# probes_headers.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_headers.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_headers.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_headers.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_headers.d"; then
+        echo "Found probes for headers $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_headers.d $realobjects && cp probes_headers.o .libs/probes_headers.o && cp probes_headers.libtool.hack probes_headers.lo"
+        DTRACE_OBJ=probes_headers.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_headers.h -s $modpath_current/probes_headers.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_headers.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_headers.o .libs/probes_headers.o && cp probes_headers.libtool.hack probes_headers.lo"
+        DTRACE_OBJ=probes_headers.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_HEADERS_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_headers.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_headers.d"; then
+        echo "Found probes for headers $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_headers.d $realobjects && cp probes_headers.o .libs/probes_headers.o && cp probes_headers.libtool.hack probes_headers.lo"
+        DTRACE_OBJ=probes_headers.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_headers.h -s $modpath_current/probes_headers.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_headers.so $realobjects .libs/probes_headers.o \$(MOD_HEADERS_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_headers.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_headers.o .libs/probes_headers.o && cp probes_headers.libtool.hack probes_headers.lo"
+        DTRACE_OBJ=probes_headers.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_headers.so $realobjects .libs/probes_headers.o \$(MOD_HEADERS_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_HEADERS_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_HEADERS_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -12074,23 +13850,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_ident.libtool.hack<<EOF
+# probes_ident.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_ident.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_ident.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_ident.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_ident.d"; then
+        echo "Found probes for ident $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_ident.d $realobjects && cp probes_ident.o .libs/probes_ident.o && cp probes_ident.libtool.hack probes_ident.lo"
+        DTRACE_OBJ=probes_ident.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_ident.h -s $modpath_current/probes_ident.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_ident.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_ident.o .libs/probes_ident.o && cp probes_ident.libtool.hack probes_ident.lo"
+        DTRACE_OBJ=probes_ident.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_IDENT_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_ident.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_ident.d"; then
+        echo "Found probes for ident $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_ident.d $realobjects && cp probes_ident.o .libs/probes_ident.o && cp probes_ident.libtool.hack probes_ident.lo"
+        DTRACE_OBJ=probes_ident.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_ident.h -s $modpath_current/probes_ident.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_ident.so $realobjects .libs/probes_ident.o \$(MOD_IDENT_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_ident.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_ident.o .libs/probes_ident.o && cp probes_ident.libtool.hack probes_ident.lo"
+        DTRACE_OBJ=probes_ident.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_ident.so $realobjects .libs/probes_ident.o \$(MOD_IDENT_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_IDENT_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_IDENT_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -12203,23 +14019,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_usertrack.libtool.hack<<EOF
+# probes_usertrack.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_usertrack.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_usertrack.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_usertrack.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_usertrack.d"; then
+        echo "Found probes for usertrack $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_usertrack.d $realobjects && cp probes_usertrack.o .libs/probes_usertrack.o && cp probes_usertrack.libtool.hack probes_usertrack.lo"
+        DTRACE_OBJ=probes_usertrack.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_usertrack.h -s $modpath_current/probes_usertrack.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_usertrack.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_usertrack.o .libs/probes_usertrack.o && cp probes_usertrack.libtool.hack probes_usertrack.lo"
+        DTRACE_OBJ=probes_usertrack.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_USERTRACK_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_usertrack.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_usertrack.d"; then
+        echo "Found probes for usertrack $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_usertrack.d $realobjects && cp probes_usertrack.o .libs/probes_usertrack.o && cp probes_usertrack.libtool.hack probes_usertrack.lo"
+        DTRACE_OBJ=probes_usertrack.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_usertrack.h -s $modpath_current/probes_usertrack.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_usertrack.so $realobjects .libs/probes_usertrack.o \$(MOD_USERTRACK_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_usertrack.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_usertrack.o .libs/probes_usertrack.o && cp probes_usertrack.libtool.hack probes_usertrack.lo"
+        DTRACE_OBJ=probes_usertrack.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_usertrack.so $realobjects .libs/probes_usertrack.o \$(MOD_USERTRACK_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_USERTRACK_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_USERTRACK_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -12297,23 +14153,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_unique_id.libtool.hack<<EOF
+# probes_unique_id.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_unique_id.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_unique_id.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_unique_id.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_unique_id.d"; then
+        echo "Found probes for unique_id $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_unique_id.d $realobjects && cp probes_unique_id.o .libs/probes_unique_id.o && cp probes_unique_id.libtool.hack probes_unique_id.lo"
+        DTRACE_OBJ=probes_unique_id.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_unique_id.h -s $modpath_current/probes_unique_id.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_unique_id.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_unique_id.o .libs/probes_unique_id.o && cp probes_unique_id.libtool.hack probes_unique_id.lo"
+        DTRACE_OBJ=probes_unique_id.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_UNIQUE_ID_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_unique_id.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_unique_id.d"; then
+        echo "Found probes for unique_id $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_unique_id.d $realobjects && cp probes_unique_id.o .libs/probes_unique_id.o && cp probes_unique_id.libtool.hack probes_unique_id.lo"
+        DTRACE_OBJ=probes_unique_id.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_unique_id.h -s $modpath_current/probes_unique_id.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_unique_id.so $realobjects .libs/probes_unique_id.o \$(MOD_UNIQUE_ID_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_unique_id.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_unique_id.o .libs/probes_unique_id.o && cp probes_unique_id.libtool.hack probes_unique_id.lo"
+        DTRACE_OBJ=probes_unique_id.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_unique_id.so $realobjects .libs/probes_unique_id.o \$(MOD_UNIQUE_ID_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_UNIQUE_ID_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_UNIQUE_ID_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -12390,23 +14286,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_setenvif.libtool.hack<<EOF
+# probes_setenvif.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_setenvif.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_setenvif.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_setenvif.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_setenvif.d"; then
+        echo "Found probes for setenvif $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_setenvif.d $realobjects && cp probes_setenvif.o .libs/probes_setenvif.o && cp probes_setenvif.libtool.hack probes_setenvif.lo"
+        DTRACE_OBJ=probes_setenvif.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_setenvif.h -s $modpath_current/probes_setenvif.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_setenvif.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_setenvif.o .libs/probes_setenvif.o && cp probes_setenvif.libtool.hack probes_setenvif.lo"
+        DTRACE_OBJ=probes_setenvif.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_SETENVIF_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_setenvif.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_setenvif.d"; then
+        echo "Found probes for setenvif $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_setenvif.d $realobjects && cp probes_setenvif.o .libs/probes_setenvif.o && cp probes_setenvif.libtool.hack probes_setenvif.lo"
+        DTRACE_OBJ=probes_setenvif.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_setenvif.h -s $modpath_current/probes_setenvif.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_setenvif.so $realobjects .libs/probes_setenvif.o \$(MOD_SETENVIF_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_setenvif.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_setenvif.o .libs/probes_setenvif.o && cp probes_setenvif.libtool.hack probes_setenvif.lo"
+        DTRACE_OBJ=probes_setenvif.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_setenvif.so $realobjects .libs/probes_setenvif.o \$(MOD_SETENVIF_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_SETENVIF_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_SETENVIF_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -12483,23 +14419,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_version.libtool.hack<<EOF
+# probes_version.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_version.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_version.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_version.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_version.d"; then
+        echo "Found probes for version $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_version.d $realobjects && cp probes_version.o .libs/probes_version.o && cp probes_version.libtool.hack probes_version.lo"
+        DTRACE_OBJ=probes_version.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_version.h -s $modpath_current/probes_version.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_version.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_version.o .libs/probes_version.o && cp probes_version.libtool.hack probes_version.lo"
+        DTRACE_OBJ=probes_version.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_VERSION_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_version.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_version.d"; then
+        echo "Found probes for version $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_version.d $realobjects && cp probes_version.o .libs/probes_version.o && cp probes_version.libtool.hack probes_version.lo"
+        DTRACE_OBJ=probes_version.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_version.h -s $modpath_current/probes_version.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_version.so $realobjects .libs/probes_version.o \$(MOD_VERSION_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_version.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_version.o .libs/probes_version.o && cp probes_version.libtool.hack probes_version.lo"
+        DTRACE_OBJ=probes_version.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_version.so $realobjects .libs/probes_version.o \$(MOD_VERSION_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_VERSION_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_VERSION_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -12610,23 +14586,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_proxy.libtool.hack<<EOF
+# probes_proxy.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_proxy.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_proxy.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_proxy.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_proxy.d"; then
+        echo "Found probes for proxy $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_proxy.d $realobjects && cp probes_proxy.o .libs/probes_proxy.o && cp probes_proxy.libtool.hack probes_proxy.lo"
+        DTRACE_OBJ=probes_proxy.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_proxy.h -s $modpath_current/probes_proxy.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_proxy.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_proxy.o .libs/probes_proxy.o && cp probes_proxy.libtool.hack probes_proxy.lo"
+        DTRACE_OBJ=probes_proxy.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_PROXY_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_proxy.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_proxy.d"; then
+        echo "Found probes for proxy $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_proxy.d $realobjects && cp probes_proxy.o .libs/probes_proxy.o && cp probes_proxy.libtool.hack probes_proxy.lo"
+        DTRACE_OBJ=probes_proxy.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_proxy.h -s $modpath_current/probes_proxy.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_proxy.so $realobjects .libs/probes_proxy.o \$(MOD_PROXY_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_proxy.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_proxy.o .libs/probes_proxy.o && cp probes_proxy.libtool.hack probes_proxy.lo"
+        DTRACE_OBJ=probes_proxy.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_proxy.so $realobjects .libs/probes_proxy.o \$(MOD_PROXY_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_PROXY_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_PROXY_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -12724,23 +14740,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_proxy_connect.libtool.hack<<EOF
+# probes_proxy_connect.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_proxy_connect.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_proxy_connect.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_proxy_connect.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_proxy_connect.d"; then
+        echo "Found probes for proxy_connect $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_proxy_connect.d $realobjects && cp probes_proxy_connect.o .libs/probes_proxy_connect.o && cp probes_proxy_connect.libtool.hack probes_proxy_connect.lo"
+        DTRACE_OBJ=probes_proxy_connect.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_proxy_connect.h -s $modpath_current/probes_proxy_connect.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_proxy_connect.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_proxy_connect.o .libs/probes_proxy_connect.o && cp probes_proxy_connect.libtool.hack probes_proxy_connect.lo"
+        DTRACE_OBJ=probes_proxy_connect.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_PROXY_CONNECT_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_proxy_connect.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_proxy_connect.d"; then
+        echo "Found probes for proxy_connect $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_proxy_connect.d $realobjects && cp probes_proxy_connect.o .libs/probes_proxy_connect.o && cp probes_proxy_connect.libtool.hack probes_proxy_connect.lo"
+        DTRACE_OBJ=probes_proxy_connect.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_proxy_connect.h -s $modpath_current/probes_proxy_connect.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_proxy_connect.so $realobjects .libs/probes_proxy_connect.o \$(MOD_PROXY_CONNECT_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_proxy_connect.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_proxy_connect.o .libs/probes_proxy_connect.o && cp probes_proxy_connect.libtool.hack probes_proxy_connect.lo"
+        DTRACE_OBJ=probes_proxy_connect.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_proxy_connect.so $realobjects .libs/probes_proxy_connect.o \$(MOD_PROXY_CONNECT_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_PROXY_CONNECT_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_PROXY_CONNECT_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -12817,23 +14873,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_proxy_ftp.libtool.hack<<EOF
+# probes_proxy_ftp.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_proxy_ftp.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_proxy_ftp.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_proxy_ftp.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_proxy_ftp.d"; then
+        echo "Found probes for proxy_ftp $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_proxy_ftp.d $realobjects && cp probes_proxy_ftp.o .libs/probes_proxy_ftp.o && cp probes_proxy_ftp.libtool.hack probes_proxy_ftp.lo"
+        DTRACE_OBJ=probes_proxy_ftp.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_proxy_ftp.h -s $modpath_current/probes_proxy_ftp.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_proxy_ftp.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_proxy_ftp.o .libs/probes_proxy_ftp.o && cp probes_proxy_ftp.libtool.hack probes_proxy_ftp.lo"
+        DTRACE_OBJ=probes_proxy_ftp.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_PROXY_FTP_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_proxy_ftp.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_proxy_ftp.d"; then
+        echo "Found probes for proxy_ftp $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_proxy_ftp.d $realobjects && cp probes_proxy_ftp.o .libs/probes_proxy_ftp.o && cp probes_proxy_ftp.libtool.hack probes_proxy_ftp.lo"
+        DTRACE_OBJ=probes_proxy_ftp.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_proxy_ftp.h -s $modpath_current/probes_proxy_ftp.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_proxy_ftp.so $realobjects .libs/probes_proxy_ftp.o \$(MOD_PROXY_FTP_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_proxy_ftp.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_proxy_ftp.o .libs/probes_proxy_ftp.o && cp probes_proxy_ftp.libtool.hack probes_proxy_ftp.lo"
+        DTRACE_OBJ=probes_proxy_ftp.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_proxy_ftp.so $realobjects .libs/probes_proxy_ftp.o \$(MOD_PROXY_FTP_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_PROXY_FTP_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_PROXY_FTP_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -12910,23 +15006,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_proxy_http.libtool.hack<<EOF
+# probes_proxy_http.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_proxy_http.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_proxy_http.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_proxy_http.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_proxy_http.d"; then
+        echo "Found probes for proxy_http $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_proxy_http.d $realobjects && cp probes_proxy_http.o .libs/probes_proxy_http.o && cp probes_proxy_http.libtool.hack probes_proxy_http.lo"
+        DTRACE_OBJ=probes_proxy_http.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_proxy_http.h -s $modpath_current/probes_proxy_http.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_proxy_http.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_proxy_http.o .libs/probes_proxy_http.o && cp probes_proxy_http.libtool.hack probes_proxy_http.lo"
+        DTRACE_OBJ=probes_proxy_http.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_PROXY_HTTP_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_proxy_http.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_proxy_http.d"; then
+        echo "Found probes for proxy_http $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_proxy_http.d $realobjects && cp probes_proxy_http.o .libs/probes_proxy_http.o && cp probes_proxy_http.libtool.hack probes_proxy_http.lo"
+        DTRACE_OBJ=probes_proxy_http.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_proxy_http.h -s $modpath_current/probes_proxy_http.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_proxy_http.so $realobjects .libs/probes_proxy_http.o \$(MOD_PROXY_HTTP_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_proxy_http.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_proxy_http.o .libs/probes_proxy_http.o && cp probes_proxy_http.libtool.hack probes_proxy_http.lo"
+        DTRACE_OBJ=probes_proxy_http.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_proxy_http.so $realobjects .libs/probes_proxy_http.o \$(MOD_PROXY_HTTP_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_PROXY_HTTP_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_PROXY_HTTP_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -13096,23 +15232,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_proxy_ajp.libtool.hack<<EOF
+# probes_proxy_ajp.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_proxy_ajp.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_proxy_ajp.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_proxy_ajp.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_proxy_ajp.d"; then
+        echo "Found probes for proxy_ajp $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_proxy_ajp.d $realobjects && cp probes_proxy_ajp.o .libs/probes_proxy_ajp.o && cp probes_proxy_ajp.libtool.hack probes_proxy_ajp.lo"
+        DTRACE_OBJ=probes_proxy_ajp.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_proxy_ajp.h -s $modpath_current/probes_proxy_ajp.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_proxy_ajp.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_proxy_ajp.o .libs/probes_proxy_ajp.o && cp probes_proxy_ajp.libtool.hack probes_proxy_ajp.lo"
+        DTRACE_OBJ=probes_proxy_ajp.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_PROXY_AJP_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_proxy_ajp.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_proxy_ajp.d"; then
+        echo "Found probes for proxy_ajp $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_proxy_ajp.d $realobjects && cp probes_proxy_ajp.o .libs/probes_proxy_ajp.o && cp probes_proxy_ajp.libtool.hack probes_proxy_ajp.lo"
+        DTRACE_OBJ=probes_proxy_ajp.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_proxy_ajp.h -s $modpath_current/probes_proxy_ajp.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_proxy_ajp.so $realobjects .libs/probes_proxy_ajp.o \$(MOD_PROXY_AJP_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_proxy_ajp.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_proxy_ajp.o .libs/probes_proxy_ajp.o && cp probes_proxy_ajp.libtool.hack probes_proxy_ajp.lo"
+        DTRACE_OBJ=probes_proxy_ajp.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_proxy_ajp.so $realobjects .libs/probes_proxy_ajp.o \$(MOD_PROXY_AJP_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_PROXY_AJP_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_PROXY_AJP_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -13189,23 +15365,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_proxy_balancer.libtool.hack<<EOF
+# probes_proxy_balancer.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_proxy_balancer.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_proxy_balancer.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_proxy_balancer.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_proxy_balancer.d"; then
+        echo "Found probes for proxy_balancer $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_proxy_balancer.d $realobjects && cp probes_proxy_balancer.o .libs/probes_proxy_balancer.o && cp probes_proxy_balancer.libtool.hack probes_proxy_balancer.lo"
+        DTRACE_OBJ=probes_proxy_balancer.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_proxy_balancer.h -s $modpath_current/probes_proxy_balancer.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_proxy_balancer.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_proxy_balancer.o .libs/probes_proxy_balancer.o && cp probes_proxy_balancer.libtool.hack probes_proxy_balancer.lo"
+        DTRACE_OBJ=probes_proxy_balancer.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_PROXY_BALANCER_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_proxy_balancer.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_proxy_balancer.d"; then
+        echo "Found probes for proxy_balancer $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_proxy_balancer.d $realobjects && cp probes_proxy_balancer.o .libs/probes_proxy_balancer.o && cp probes_proxy_balancer.libtool.hack probes_proxy_balancer.lo"
+        DTRACE_OBJ=probes_proxy_balancer.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_proxy_balancer.h -s $modpath_current/probes_proxy_balancer.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_proxy_balancer.so $realobjects .libs/probes_proxy_balancer.o \$(MOD_PROXY_BALANCER_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_proxy_balancer.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_proxy_balancer.o .libs/probes_proxy_balancer.o && cp probes_proxy_balancer.libtool.hack probes_proxy_balancer.lo"
+        DTRACE_OBJ=probes_proxy_balancer.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_proxy_balancer.so $realobjects .libs/probes_proxy_balancer.o \$(MOD_PROXY_BALANCER_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_PROXY_BALANCER_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_PROXY_BALANCER_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -13951,23 +16167,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_ssl.libtool.hack<<EOF
+# probes_ssl.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_ssl.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_ssl.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_ssl.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_ssl.d"; then
+        echo "Found probes for ssl $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_ssl.d $realobjects && cp probes_ssl.o .libs/probes_ssl.o && cp probes_ssl.libtool.hack probes_ssl.lo"
+        DTRACE_OBJ=probes_ssl.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_ssl.h -s $modpath_current/probes_ssl.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_ssl.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_ssl.o .libs/probes_ssl.o && cp probes_ssl.libtool.hack probes_ssl.lo"
+        DTRACE_OBJ=probes_ssl.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_SSL_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_ssl.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_ssl.d"; then
+        echo "Found probes for ssl $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_ssl.d $realobjects && cp probes_ssl.o .libs/probes_ssl.o && cp probes_ssl.libtool.hack probes_ssl.lo"
+        DTRACE_OBJ=probes_ssl.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_ssl.h -s $modpath_current/probes_ssl.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_ssl.so $realobjects .libs/probes_ssl.o \$(MOD_SSL_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_ssl.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_ssl.o .libs/probes_ssl.o && cp probes_ssl.libtool.hack probes_ssl.lo"
+        DTRACE_OBJ=probes_ssl.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_ssl.so $realobjects .libs/probes_ssl.o \$(MOD_SSL_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_SSL_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_SSL_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -14093,23 +16349,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_optional_hook_export.libtool.hack<<EOF
+# probes_optional_hook_export.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_optional_hook_export.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_optional_hook_export.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_optional_hook_export.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_optional_hook_export.d"; then
+        echo "Found probes for optional_hook_export $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_optional_hook_export.d $realobjects && cp probes_optional_hook_export.o .libs/probes_optional_hook_export.o && cp probes_optional_hook_export.libtool.hack probes_optional_hook_export.lo"
+        DTRACE_OBJ=probes_optional_hook_export.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_optional_hook_export.h -s $modpath_current/probes_optional_hook_export.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_optional_hook_export.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_optional_hook_export.o .libs/probes_optional_hook_export.o && cp probes_optional_hook_export.libtool.hack probes_optional_hook_export.lo"
+        DTRACE_OBJ=probes_optional_hook_export.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_OPTIONAL_HOOK_EXPORT_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_optional_hook_export.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_optional_hook_export.d"; then
+        echo "Found probes for optional_hook_export $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_optional_hook_export.d $realobjects && cp probes_optional_hook_export.o .libs/probes_optional_hook_export.o && cp probes_optional_hook_export.libtool.hack probes_optional_hook_export.lo"
+        DTRACE_OBJ=probes_optional_hook_export.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_optional_hook_export.h -s $modpath_current/probes_optional_hook_export.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_optional_hook_export.so $realobjects .libs/probes_optional_hook_export.o \$(MOD_OPTIONAL_HOOK_EXPORT_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_optional_hook_export.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_optional_hook_export.o .libs/probes_optional_hook_export.o && cp probes_optional_hook_export.libtool.hack probes_optional_hook_export.lo"
+        DTRACE_OBJ=probes_optional_hook_export.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_optional_hook_export.so $realobjects .libs/probes_optional_hook_export.o \$(MOD_OPTIONAL_HOOK_EXPORT_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_OPTIONAL_HOOK_EXPORT_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_OPTIONAL_HOOK_EXPORT_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -14186,23 +16482,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_optional_hook_import.libtool.hack<<EOF
+# probes_optional_hook_import.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_optional_hook_import.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_optional_hook_import.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_optional_hook_import.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_optional_hook_import.d"; then
+        echo "Found probes for optional_hook_import $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_optional_hook_import.d $realobjects && cp probes_optional_hook_import.o .libs/probes_optional_hook_import.o && cp probes_optional_hook_import.libtool.hack probes_optional_hook_import.lo"
+        DTRACE_OBJ=probes_optional_hook_import.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_optional_hook_import.h -s $modpath_current/probes_optional_hook_import.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_optional_hook_import.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_optional_hook_import.o .libs/probes_optional_hook_import.o && cp probes_optional_hook_import.libtool.hack probes_optional_hook_import.lo"
+        DTRACE_OBJ=probes_optional_hook_import.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_OPTIONAL_HOOK_IMPORT_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_optional_hook_import.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_optional_hook_import.d"; then
+        echo "Found probes for optional_hook_import $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_optional_hook_import.d $realobjects && cp probes_optional_hook_import.o .libs/probes_optional_hook_import.o && cp probes_optional_hook_import.libtool.hack probes_optional_hook_import.lo"
+        DTRACE_OBJ=probes_optional_hook_import.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_optional_hook_import.h -s $modpath_current/probes_optional_hook_import.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_optional_hook_import.so $realobjects .libs/probes_optional_hook_import.o \$(MOD_OPTIONAL_HOOK_IMPORT_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_optional_hook_import.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_optional_hook_import.o .libs/probes_optional_hook_import.o && cp probes_optional_hook_import.libtool.hack probes_optional_hook_import.lo"
+        DTRACE_OBJ=probes_optional_hook_import.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_optional_hook_import.so $realobjects .libs/probes_optional_hook_import.o \$(MOD_OPTIONAL_HOOK_IMPORT_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_OPTIONAL_HOOK_IMPORT_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_OPTIONAL_HOOK_IMPORT_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -14279,23 +16615,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_optional_fn_import.libtool.hack<<EOF
+# probes_optional_fn_import.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_optional_fn_import.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_optional_fn_import.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_optional_fn_import.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_optional_fn_import.d"; then
+        echo "Found probes for optional_fn_import $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_optional_fn_import.d $realobjects && cp probes_optional_fn_import.o .libs/probes_optional_fn_import.o && cp probes_optional_fn_import.libtool.hack probes_optional_fn_import.lo"
+        DTRACE_OBJ=probes_optional_fn_import.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_optional_fn_import.h -s $modpath_current/probes_optional_fn_import.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_optional_fn_import.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_optional_fn_import.o .libs/probes_optional_fn_import.o && cp probes_optional_fn_import.libtool.hack probes_optional_fn_import.lo"
+        DTRACE_OBJ=probes_optional_fn_import.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_OPTIONAL_FN_IMPORT_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_optional_fn_import.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_optional_fn_import.d"; then
+        echo "Found probes for optional_fn_import $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_optional_fn_import.d $realobjects && cp probes_optional_fn_import.o .libs/probes_optional_fn_import.o && cp probes_optional_fn_import.libtool.hack probes_optional_fn_import.lo"
+        DTRACE_OBJ=probes_optional_fn_import.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_optional_fn_import.h -s $modpath_current/probes_optional_fn_import.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_optional_fn_import.so $realobjects .libs/probes_optional_fn_import.o \$(MOD_OPTIONAL_FN_IMPORT_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_optional_fn_import.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_optional_fn_import.o .libs/probes_optional_fn_import.o && cp probes_optional_fn_import.libtool.hack probes_optional_fn_import.lo"
+        DTRACE_OBJ=probes_optional_fn_import.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_optional_fn_import.so $realobjects .libs/probes_optional_fn_import.o \$(MOD_OPTIONAL_FN_IMPORT_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_OPTIONAL_FN_IMPORT_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_OPTIONAL_FN_IMPORT_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -14372,23 +16748,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_optional_fn_export.libtool.hack<<EOF
+# probes_optional_fn_export.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_optional_fn_export.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_optional_fn_export.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_optional_fn_export.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_optional_fn_export.d"; then
+        echo "Found probes for optional_fn_export $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_optional_fn_export.d $realobjects && cp probes_optional_fn_export.o .libs/probes_optional_fn_export.o && cp probes_optional_fn_export.libtool.hack probes_optional_fn_export.lo"
+        DTRACE_OBJ=probes_optional_fn_export.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_optional_fn_export.h -s $modpath_current/probes_optional_fn_export.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_optional_fn_export.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_optional_fn_export.o .libs/probes_optional_fn_export.o && cp probes_optional_fn_export.libtool.hack probes_optional_fn_export.lo"
+        DTRACE_OBJ=probes_optional_fn_export.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_OPTIONAL_FN_EXPORT_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_optional_fn_export.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_optional_fn_export.d"; then
+        echo "Found probes for optional_fn_export $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_optional_fn_export.d $realobjects && cp probes_optional_fn_export.o .libs/probes_optional_fn_export.o && cp probes_optional_fn_export.libtool.hack probes_optional_fn_export.lo"
+        DTRACE_OBJ=probes_optional_fn_export.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_optional_fn_export.h -s $modpath_current/probes_optional_fn_export.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_optional_fn_export.so $realobjects .libs/probes_optional_fn_export.o \$(MOD_OPTIONAL_FN_EXPORT_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_optional_fn_export.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_optional_fn_export.o .libs/probes_optional_fn_export.o && cp probes_optional_fn_export.libtool.hack probes_optional_fn_export.lo"
+        DTRACE_OBJ=probes_optional_fn_export.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_optional_fn_export.so $realobjects .libs/probes_optional_fn_export.o \$(MOD_OPTIONAL_FN_EXPORT_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_OPTIONAL_FN_EXPORT_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_OPTIONAL_FN_EXPORT_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -15558,23 +17974,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_http.libtool.hack<<EOF
+# probes_http.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_http.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_http.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_http.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_http.d"; then
+        echo "Found probes for http $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_http.d $realobjects && cp probes_http.o .libs/probes_http.o && cp probes_http.libtool.hack probes_http.lo"
+        DTRACE_OBJ=probes_http.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_http.h -s $modpath_current/probes_http.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_http.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_http.o .libs/probes_http.o && cp probes_http.libtool.hack probes_http.lo"
+        DTRACE_OBJ=probes_http.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_HTTP_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_http.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_http.d"; then
+        echo "Found probes for http $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_http.d $realobjects && cp probes_http.o .libs/probes_http.o && cp probes_http.libtool.hack probes_http.lo"
+        DTRACE_OBJ=probes_http.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_http.h -s $modpath_current/probes_http.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_http.so $realobjects .libs/probes_http.o \$(MOD_HTTP_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_http.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_http.o .libs/probes_http.o && cp probes_http.libtool.hack probes_http.lo"
+        DTRACE_OBJ=probes_http.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_http.so $realobjects .libs/probes_http.o \$(MOD_HTTP_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_HTTP_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_HTTP_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -15651,23 +18107,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_mime.libtool.hack<<EOF
+# probes_mime.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_mime.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_mime.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_mime.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_mime.d"; then
+        echo "Found probes for mime $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_mime.d $realobjects && cp probes_mime.o .libs/probes_mime.o && cp probes_mime.libtool.hack probes_mime.lo"
+        DTRACE_OBJ=probes_mime.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_mime.h -s $modpath_current/probes_mime.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_mime.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_mime.o .libs/probes_mime.o && cp probes_mime.libtool.hack probes_mime.lo"
+        DTRACE_OBJ=probes_mime.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_MIME_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_mime.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_mime.d"; then
+        echo "Found probes for mime $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_mime.d $realobjects && cp probes_mime.o .libs/probes_mime.o && cp probes_mime.libtool.hack probes_mime.lo"
+        DTRACE_OBJ=probes_mime.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_mime.h -s $modpath_current/probes_mime.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_mime.so $realobjects .libs/probes_mime.o \$(MOD_MIME_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_mime.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_mime.o .libs/probes_mime.o && cp probes_mime.libtool.hack probes_mime.lo"
+        DTRACE_OBJ=probes_mime.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_mime.so $realobjects .libs/probes_mime.o \$(MOD_MIME_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_MIME_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_MIME_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -15843,23 +18339,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_dav.libtool.hack<<EOF
+# probes_dav.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_dav.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_dav.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_dav.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_dav.d"; then
+        echo "Found probes for dav $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_dav.d $realobjects && cp probes_dav.o .libs/probes_dav.o && cp probes_dav.libtool.hack probes_dav.lo"
+        DTRACE_OBJ=probes_dav.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_dav.h -s $modpath_current/probes_dav.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_dav.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_dav.o .libs/probes_dav.o && cp probes_dav.libtool.hack probes_dav.lo"
+        DTRACE_OBJ=probes_dav.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_DAV_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_dav.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_dav.d"; then
+        echo "Found probes for dav $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_dav.d $realobjects && cp probes_dav.o .libs/probes_dav.o && cp probes_dav.libtool.hack probes_dav.lo"
+        DTRACE_OBJ=probes_dav.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_dav.h -s $modpath_current/probes_dav.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_dav.so $realobjects .libs/probes_dav.o \$(MOD_DAV_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_dav.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_dav.o .libs/probes_dav.o && cp probes_dav.libtool.hack probes_dav.lo"
+        DTRACE_OBJ=probes_dav.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_dav.so $realobjects .libs/probes_dav.o \$(MOD_DAV_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_DAV_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_DAV_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -15989,23 +18525,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_status.libtool.hack<<EOF
+# probes_status.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_status.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_status.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_status.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_status.d"; then
+        echo "Found probes for status $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_status.d $realobjects && cp probes_status.o .libs/probes_status.o && cp probes_status.libtool.hack probes_status.lo"
+        DTRACE_OBJ=probes_status.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_status.h -s $modpath_current/probes_status.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_status.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_status.o .libs/probes_status.o && cp probes_status.libtool.hack probes_status.lo"
+        DTRACE_OBJ=probes_status.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_STATUS_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_status.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_status.d"; then
+        echo "Found probes for status $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_status.d $realobjects && cp probes_status.o .libs/probes_status.o && cp probes_status.libtool.hack probes_status.lo"
+        DTRACE_OBJ=probes_status.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_status.h -s $modpath_current/probes_status.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_status.so $realobjects .libs/probes_status.o \$(MOD_STATUS_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_status.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_status.o .libs/probes_status.o && cp probes_status.libtool.hack probes_status.lo"
+        DTRACE_OBJ=probes_status.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_status.so $realobjects .libs/probes_status.o \$(MOD_STATUS_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_STATUS_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_STATUS_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -16082,23 +18658,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_autoindex.libtool.hack<<EOF
+# probes_autoindex.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_autoindex.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_autoindex.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_autoindex.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_autoindex.d"; then
+        echo "Found probes for autoindex $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_autoindex.d $realobjects && cp probes_autoindex.o .libs/probes_autoindex.o && cp probes_autoindex.libtool.hack probes_autoindex.lo"
+        DTRACE_OBJ=probes_autoindex.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_autoindex.h -s $modpath_current/probes_autoindex.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_autoindex.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_autoindex.o .libs/probes_autoindex.o && cp probes_autoindex.libtool.hack probes_autoindex.lo"
+        DTRACE_OBJ=probes_autoindex.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_AUTOINDEX_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_autoindex.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_autoindex.d"; then
+        echo "Found probes for autoindex $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_autoindex.d $realobjects && cp probes_autoindex.o .libs/probes_autoindex.o && cp probes_autoindex.libtool.hack probes_autoindex.lo"
+        DTRACE_OBJ=probes_autoindex.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_autoindex.h -s $modpath_current/probes_autoindex.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_autoindex.so $realobjects .libs/probes_autoindex.o \$(MOD_AUTOINDEX_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_autoindex.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_autoindex.o .libs/probes_autoindex.o && cp probes_autoindex.libtool.hack probes_autoindex.lo"
+        DTRACE_OBJ=probes_autoindex.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_autoindex.so $realobjects .libs/probes_autoindex.o \$(MOD_AUTOINDEX_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_AUTOINDEX_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_AUTOINDEX_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -16175,23 +18791,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_asis.libtool.hack<<EOF
+# probes_asis.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_asis.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_asis.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_asis.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_asis.d"; then
+        echo "Found probes for asis $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_asis.d $realobjects && cp probes_asis.o .libs/probes_asis.o && cp probes_asis.libtool.hack probes_asis.lo"
+        DTRACE_OBJ=probes_asis.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_asis.h -s $modpath_current/probes_asis.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_asis.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_asis.o .libs/probes_asis.o && cp probes_asis.libtool.hack probes_asis.lo"
+        DTRACE_OBJ=probes_asis.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_ASIS_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_asis.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_asis.d"; then
+        echo "Found probes for asis $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_asis.d $realobjects && cp probes_asis.o .libs/probes_asis.o && cp probes_asis.libtool.hack probes_asis.lo"
+        DTRACE_OBJ=probes_asis.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_asis.h -s $modpath_current/probes_asis.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_asis.so $realobjects .libs/probes_asis.o \$(MOD_ASIS_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_asis.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_asis.o .libs/probes_asis.o && cp probes_asis.libtool.hack probes_asis.lo"
+        DTRACE_OBJ=probes_asis.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_asis.so $realobjects .libs/probes_asis.o \$(MOD_ASIS_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_ASIS_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_ASIS_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -16268,23 +18924,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_info.libtool.hack<<EOF
+# probes_info.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_info.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_info.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_info.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_info.d"; then
+        echo "Found probes for info $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_info.d $realobjects && cp probes_info.o .libs/probes_info.o && cp probes_info.libtool.hack probes_info.lo"
+        DTRACE_OBJ=probes_info.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_info.h -s $modpath_current/probes_info.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_info.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_info.o .libs/probes_info.o && cp probes_info.libtool.hack probes_info.lo"
+        DTRACE_OBJ=probes_info.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_INFO_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_info.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_info.d"; then
+        echo "Found probes for info $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_info.d $realobjects && cp probes_info.o .libs/probes_info.o && cp probes_info.libtool.hack probes_info.lo"
+        DTRACE_OBJ=probes_info.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_info.h -s $modpath_current/probes_info.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_info.so $realobjects .libs/probes_info.o \$(MOD_INFO_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_info.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_info.o .libs/probes_info.o && cp probes_info.libtool.hack probes_info.lo"
+        DTRACE_OBJ=probes_info.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_info.so $realobjects .libs/probes_info.o \$(MOD_INFO_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_INFO_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_INFO_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -16373,23 +19069,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_suexec.libtool.hack<<EOF
+# probes_suexec.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_suexec.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_suexec.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_suexec.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_suexec.d"; then
+        echo "Found probes for suexec $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_suexec.d $realobjects && cp probes_suexec.o .libs/probes_suexec.o && cp probes_suexec.libtool.hack probes_suexec.lo"
+        DTRACE_OBJ=probes_suexec.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_suexec.h -s $modpath_current/probes_suexec.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_suexec.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_suexec.o .libs/probes_suexec.o && cp probes_suexec.libtool.hack probes_suexec.lo"
+        DTRACE_OBJ=probes_suexec.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_SUEXEC_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_suexec.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_suexec.d"; then
+        echo "Found probes for suexec $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_suexec.d $realobjects && cp probes_suexec.o .libs/probes_suexec.o && cp probes_suexec.libtool.hack probes_suexec.lo"
+        DTRACE_OBJ=probes_suexec.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_suexec.h -s $modpath_current/probes_suexec.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_suexec.so $realobjects .libs/probes_suexec.o \$(MOD_SUEXEC_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_suexec.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_suexec.o .libs/probes_suexec.o && cp probes_suexec.libtool.hack probes_suexec.lo"
+        DTRACE_OBJ=probes_suexec.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_suexec.so $realobjects .libs/probes_suexec.o \$(MOD_SUEXEC_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_SUEXEC_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_SUEXEC_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -16470,23 +19206,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_cgid.libtool.hack<<EOF
+# probes_cgid.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_cgid.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_cgid.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_cgid.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_cgid.d"; then
+        echo "Found probes for cgid $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_cgid.d $realobjects && cp probes_cgid.o .libs/probes_cgid.o && cp probes_cgid.libtool.hack probes_cgid.lo"
+        DTRACE_OBJ=probes_cgid.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_cgid.h -s $modpath_current/probes_cgid.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_cgid.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_cgid.o .libs/probes_cgid.o && cp probes_cgid.libtool.hack probes_cgid.lo"
+        DTRACE_OBJ=probes_cgid.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_CGID_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_cgid.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_cgid.d"; then
+        echo "Found probes for cgid $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_cgid.d $realobjects && cp probes_cgid.o .libs/probes_cgid.o && cp probes_cgid.libtool.hack probes_cgid.lo"
+        DTRACE_OBJ=probes_cgid.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_cgid.h -s $modpath_current/probes_cgid.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_cgid.so $realobjects .libs/probes_cgid.o \$(MOD_CGID_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_cgid.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_cgid.o .libs/probes_cgid.o && cp probes_cgid.libtool.hack probes_cgid.lo"
+        DTRACE_OBJ=probes_cgid.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_cgid.so $realobjects .libs/probes_cgid.o \$(MOD_CGID_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_CGID_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_CGID_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -16563,23 +19339,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_cgi.libtool.hack<<EOF
+# probes_cgi.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_cgi.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_cgi.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_cgi.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_cgi.d"; then
+        echo "Found probes for cgi $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_cgi.d $realobjects && cp probes_cgi.o .libs/probes_cgi.o && cp probes_cgi.libtool.hack probes_cgi.lo"
+        DTRACE_OBJ=probes_cgi.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_cgi.h -s $modpath_current/probes_cgi.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_cgi.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_cgi.o .libs/probes_cgi.o && cp probes_cgi.libtool.hack probes_cgi.lo"
+        DTRACE_OBJ=probes_cgi.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_CGI_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_cgi.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_cgi.d"; then
+        echo "Found probes for cgi $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_cgi.d $realobjects && cp probes_cgi.o .libs/probes_cgi.o && cp probes_cgi.libtool.hack probes_cgi.lo"
+        DTRACE_OBJ=probes_cgi.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_cgi.h -s $modpath_current/probes_cgi.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_cgi.so $realobjects .libs/probes_cgi.o \$(MOD_CGI_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_cgi.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_cgi.o .libs/probes_cgi.o && cp probes_cgi.libtool.hack probes_cgi.lo"
+        DTRACE_OBJ=probes_cgi.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_cgi.so $realobjects .libs/probes_cgi.o \$(MOD_CGI_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_CGI_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_CGI_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -16660,23 +19476,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_cgi.libtool.hack<<EOF
+# probes_cgi.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_cgi.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_cgi.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_cgi.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_cgi.d"; then
+        echo "Found probes for cgi $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_cgi.d $realobjects && cp probes_cgi.o .libs/probes_cgi.o && cp probes_cgi.libtool.hack probes_cgi.lo"
+        DTRACE_OBJ=probes_cgi.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_cgi.h -s $modpath_current/probes_cgi.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_cgi.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_cgi.o .libs/probes_cgi.o && cp probes_cgi.libtool.hack probes_cgi.lo"
+        DTRACE_OBJ=probes_cgi.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_CGI_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_cgi.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_cgi.d"; then
+        echo "Found probes for cgi $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_cgi.d $realobjects && cp probes_cgi.o .libs/probes_cgi.o && cp probes_cgi.libtool.hack probes_cgi.lo"
+        DTRACE_OBJ=probes_cgi.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_cgi.h -s $modpath_current/probes_cgi.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_cgi.so $realobjects .libs/probes_cgi.o \$(MOD_CGI_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_cgi.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_cgi.o .libs/probes_cgi.o && cp probes_cgi.libtool.hack probes_cgi.lo"
+        DTRACE_OBJ=probes_cgi.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_cgi.so $realobjects .libs/probes_cgi.o \$(MOD_CGI_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_CGI_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_CGI_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -16753,23 +19609,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_cgid.libtool.hack<<EOF
+# probes_cgid.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_cgid.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_cgid.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_cgid.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_cgid.d"; then
+        echo "Found probes for cgid $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_cgid.d $realobjects && cp probes_cgid.o .libs/probes_cgid.o && cp probes_cgid.libtool.hack probes_cgid.lo"
+        DTRACE_OBJ=probes_cgid.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_cgid.h -s $modpath_current/probes_cgid.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_cgid.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_cgid.o .libs/probes_cgid.o && cp probes_cgid.libtool.hack probes_cgid.lo"
+        DTRACE_OBJ=probes_cgid.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_CGID_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_cgid.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_cgid.d"; then
+        echo "Found probes for cgid $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_cgid.d $realobjects && cp probes_cgid.o .libs/probes_cgid.o && cp probes_cgid.libtool.hack probes_cgid.lo"
+        DTRACE_OBJ=probes_cgid.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_cgid.h -s $modpath_current/probes_cgid.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_cgid.so $realobjects .libs/probes_cgid.o \$(MOD_CGID_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_cgid.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_cgid.o .libs/probes_cgid.o && cp probes_cgid.libtool.hack probes_cgid.lo"
+        DTRACE_OBJ=probes_cgid.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_cgid.so $realobjects .libs/probes_cgid.o \$(MOD_CGID_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_CGID_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_CGID_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -17034,23 +19930,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_dav_fs.libtool.hack<<EOF
+# probes_dav_fs.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_dav_fs.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_dav_fs.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_dav_fs.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_dav_fs.d"; then
+        echo "Found probes for dav_fs $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_dav_fs.d $realobjects && cp probes_dav_fs.o .libs/probes_dav_fs.o && cp probes_dav_fs.libtool.hack probes_dav_fs.lo"
+        DTRACE_OBJ=probes_dav_fs.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_dav_fs.h -s $modpath_current/probes_dav_fs.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_dav_fs.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_dav_fs.o .libs/probes_dav_fs.o && cp probes_dav_fs.libtool.hack probes_dav_fs.lo"
+        DTRACE_OBJ=probes_dav_fs.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_DAV_FS_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_dav_fs.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_dav_fs.d"; then
+        echo "Found probes for dav_fs $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_dav_fs.d $realobjects && cp probes_dav_fs.o .libs/probes_dav_fs.o && cp probes_dav_fs.libtool.hack probes_dav_fs.lo"
+        DTRACE_OBJ=probes_dav_fs.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_dav_fs.h -s $modpath_current/probes_dav_fs.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_dav_fs.so $realobjects .libs/probes_dav_fs.o \$(MOD_DAV_FS_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_dav_fs.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_dav_fs.o .libs/probes_dav_fs.o && cp probes_dav_fs.libtool.hack probes_dav_fs.lo"
+        DTRACE_OBJ=probes_dav_fs.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_dav_fs.so $realobjects .libs/probes_dav_fs.o \$(MOD_DAV_FS_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_DAV_FS_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_DAV_FS_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -17162,23 +20098,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_dav_lock.libtool.hack<<EOF
+# probes_dav_lock.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_dav_lock.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_dav_lock.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_dav_lock.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_dav_lock.d"; then
+        echo "Found probes for dav_lock $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_dav_lock.d $realobjects && cp probes_dav_lock.o .libs/probes_dav_lock.o && cp probes_dav_lock.libtool.hack probes_dav_lock.lo"
+        DTRACE_OBJ=probes_dav_lock.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_dav_lock.h -s $modpath_current/probes_dav_lock.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_dav_lock.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_dav_lock.o .libs/probes_dav_lock.o && cp probes_dav_lock.libtool.hack probes_dav_lock.lo"
+        DTRACE_OBJ=probes_dav_lock.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_DAV_LOCK_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_dav_lock.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_dav_lock.d"; then
+        echo "Found probes for dav_lock $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_dav_lock.d $realobjects && cp probes_dav_lock.o .libs/probes_dav_lock.o && cp probes_dav_lock.libtool.hack probes_dav_lock.lo"
+        DTRACE_OBJ=probes_dav_lock.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_dav_lock.h -s $modpath_current/probes_dav_lock.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_dav_lock.so $realobjects .libs/probes_dav_lock.o \$(MOD_DAV_LOCK_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_dav_lock.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_dav_lock.o .libs/probes_dav_lock.o && cp probes_dav_lock.libtool.hack probes_dav_lock.lo"
+        DTRACE_OBJ=probes_dav_lock.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_dav_lock.so $realobjects .libs/probes_dav_lock.o \$(MOD_DAV_LOCK_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_DAV_LOCK_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_DAV_LOCK_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -17281,23 +20257,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_vhost_alias.libtool.hack<<EOF
+# probes_vhost_alias.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_vhost_alias.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_vhost_alias.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_vhost_alias.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_vhost_alias.d"; then
+        echo "Found probes for vhost_alias $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_vhost_alias.d $realobjects && cp probes_vhost_alias.o .libs/probes_vhost_alias.o && cp probes_vhost_alias.libtool.hack probes_vhost_alias.lo"
+        DTRACE_OBJ=probes_vhost_alias.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_vhost_alias.h -s $modpath_current/probes_vhost_alias.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_vhost_alias.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_vhost_alias.o .libs/probes_vhost_alias.o && cp probes_vhost_alias.libtool.hack probes_vhost_alias.lo"
+        DTRACE_OBJ=probes_vhost_alias.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_VHOST_ALIAS_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_vhost_alias.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_vhost_alias.d"; then
+        echo "Found probes for vhost_alias $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_vhost_alias.d $realobjects && cp probes_vhost_alias.o .libs/probes_vhost_alias.o && cp probes_vhost_alias.libtool.hack probes_vhost_alias.lo"
+        DTRACE_OBJ=probes_vhost_alias.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_vhost_alias.h -s $modpath_current/probes_vhost_alias.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_vhost_alias.so $realobjects .libs/probes_vhost_alias.o \$(MOD_VHOST_ALIAS_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_vhost_alias.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_vhost_alias.o .libs/probes_vhost_alias.o && cp probes_vhost_alias.libtool.hack probes_vhost_alias.lo"
+        DTRACE_OBJ=probes_vhost_alias.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_vhost_alias.so $realobjects .libs/probes_vhost_alias.o \$(MOD_VHOST_ALIAS_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_VHOST_ALIAS_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_VHOST_ALIAS_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -17374,23 +20390,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_negotiation.libtool.hack<<EOF
+# probes_negotiation.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_negotiation.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_negotiation.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_negotiation.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_negotiation.d"; then
+        echo "Found probes for negotiation $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_negotiation.d $realobjects && cp probes_negotiation.o .libs/probes_negotiation.o && cp probes_negotiation.libtool.hack probes_negotiation.lo"
+        DTRACE_OBJ=probes_negotiation.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_negotiation.h -s $modpath_current/probes_negotiation.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_negotiation.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_negotiation.o .libs/probes_negotiation.o && cp probes_negotiation.libtool.hack probes_negotiation.lo"
+        DTRACE_OBJ=probes_negotiation.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_NEGOTIATION_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_negotiation.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_negotiation.d"; then
+        echo "Found probes for negotiation $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_negotiation.d $realobjects && cp probes_negotiation.o .libs/probes_negotiation.o && cp probes_negotiation.libtool.hack probes_negotiation.lo"
+        DTRACE_OBJ=probes_negotiation.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_negotiation.h -s $modpath_current/probes_negotiation.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_negotiation.so $realobjects .libs/probes_negotiation.o \$(MOD_NEGOTIATION_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_negotiation.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_negotiation.o .libs/probes_negotiation.o && cp probes_negotiation.libtool.hack probes_negotiation.lo"
+        DTRACE_OBJ=probes_negotiation.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_negotiation.so $realobjects .libs/probes_negotiation.o \$(MOD_NEGOTIATION_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_NEGOTIATION_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_NEGOTIATION_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -17467,23 +20523,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_dir.libtool.hack<<EOF
+# probes_dir.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_dir.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_dir.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_dir.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_dir.d"; then
+        echo "Found probes for dir $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_dir.d $realobjects && cp probes_dir.o .libs/probes_dir.o && cp probes_dir.libtool.hack probes_dir.lo"
+        DTRACE_OBJ=probes_dir.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_dir.h -s $modpath_current/probes_dir.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_dir.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_dir.o .libs/probes_dir.o && cp probes_dir.libtool.hack probes_dir.lo"
+        DTRACE_OBJ=probes_dir.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_DIR_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_dir.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_dir.d"; then
+        echo "Found probes for dir $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_dir.d $realobjects && cp probes_dir.o .libs/probes_dir.o && cp probes_dir.libtool.hack probes_dir.lo"
+        DTRACE_OBJ=probes_dir.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_dir.h -s $modpath_current/probes_dir.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_dir.so $realobjects .libs/probes_dir.o \$(MOD_DIR_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_dir.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_dir.o .libs/probes_dir.o && cp probes_dir.libtool.hack probes_dir.lo"
+        DTRACE_OBJ=probes_dir.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_dir.so $realobjects .libs/probes_dir.o \$(MOD_DIR_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_DIR_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_DIR_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -17560,23 +20656,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_imagemap.libtool.hack<<EOF
+# probes_imagemap.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_imagemap.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_imagemap.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_imagemap.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_imagemap.d"; then
+        echo "Found probes for imagemap $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_imagemap.d $realobjects && cp probes_imagemap.o .libs/probes_imagemap.o && cp probes_imagemap.libtool.hack probes_imagemap.lo"
+        DTRACE_OBJ=probes_imagemap.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_imagemap.h -s $modpath_current/probes_imagemap.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_imagemap.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_imagemap.o .libs/probes_imagemap.o && cp probes_imagemap.libtool.hack probes_imagemap.lo"
+        DTRACE_OBJ=probes_imagemap.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_IMAGEMAP_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_imagemap.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_imagemap.d"; then
+        echo "Found probes for imagemap $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_imagemap.d $realobjects && cp probes_imagemap.o .libs/probes_imagemap.o && cp probes_imagemap.libtool.hack probes_imagemap.lo"
+        DTRACE_OBJ=probes_imagemap.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_imagemap.h -s $modpath_current/probes_imagemap.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_imagemap.so $realobjects .libs/probes_imagemap.o \$(MOD_IMAGEMAP_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_imagemap.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_imagemap.o .libs/probes_imagemap.o && cp probes_imagemap.libtool.hack probes_imagemap.lo"
+        DTRACE_OBJ=probes_imagemap.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_imagemap.so $realobjects .libs/probes_imagemap.o \$(MOD_IMAGEMAP_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_IMAGEMAP_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_IMAGEMAP_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -17653,23 +20789,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_actions.libtool.hack<<EOF
+# probes_actions.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_actions.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_actions.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_actions.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_actions.d"; then
+        echo "Found probes for actions $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_actions.d $realobjects && cp probes_actions.o .libs/probes_actions.o && cp probes_actions.libtool.hack probes_actions.lo"
+        DTRACE_OBJ=probes_actions.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_actions.h -s $modpath_current/probes_actions.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_actions.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_actions.o .libs/probes_actions.o && cp probes_actions.libtool.hack probes_actions.lo"
+        DTRACE_OBJ=probes_actions.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_ACTIONS_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_actions.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_actions.d"; then
+        echo "Found probes for actions $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_actions.d $realobjects && cp probes_actions.o .libs/probes_actions.o && cp probes_actions.libtool.hack probes_actions.lo"
+        DTRACE_OBJ=probes_actions.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_actions.h -s $modpath_current/probes_actions.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_actions.so $realobjects .libs/probes_actions.o \$(MOD_ACTIONS_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_actions.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_actions.o .libs/probes_actions.o && cp probes_actions.libtool.hack probes_actions.lo"
+        DTRACE_OBJ=probes_actions.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_actions.so $realobjects .libs/probes_actions.o \$(MOD_ACTIONS_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_ACTIONS_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_ACTIONS_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -17746,23 +20922,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_speling.libtool.hack<<EOF
+# probes_speling.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_speling.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_speling.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_speling.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_speling.d"; then
+        echo "Found probes for speling $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_speling.d $realobjects && cp probes_speling.o .libs/probes_speling.o && cp probes_speling.libtool.hack probes_speling.lo"
+        DTRACE_OBJ=probes_speling.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_speling.h -s $modpath_current/probes_speling.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_speling.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_speling.o .libs/probes_speling.o && cp probes_speling.libtool.hack probes_speling.lo"
+        DTRACE_OBJ=probes_speling.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_SPELING_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_speling.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_speling.d"; then
+        echo "Found probes for speling $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_speling.d $realobjects && cp probes_speling.o .libs/probes_speling.o && cp probes_speling.libtool.hack probes_speling.lo"
+        DTRACE_OBJ=probes_speling.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_speling.h -s $modpath_current/probes_speling.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_speling.so $realobjects .libs/probes_speling.o \$(MOD_SPELING_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_speling.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_speling.o .libs/probes_speling.o && cp probes_speling.libtool.hack probes_speling.lo"
+        DTRACE_OBJ=probes_speling.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_speling.so $realobjects .libs/probes_speling.o \$(MOD_SPELING_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_SPELING_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_SPELING_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -17839,23 +21055,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_userdir.libtool.hack<<EOF
+# probes_userdir.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_userdir.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_userdir.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_userdir.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_userdir.d"; then
+        echo "Found probes for userdir $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_userdir.d $realobjects && cp probes_userdir.o .libs/probes_userdir.o && cp probes_userdir.libtool.hack probes_userdir.lo"
+        DTRACE_OBJ=probes_userdir.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_userdir.h -s $modpath_current/probes_userdir.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_userdir.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_userdir.o .libs/probes_userdir.o && cp probes_userdir.libtool.hack probes_userdir.lo"
+        DTRACE_OBJ=probes_userdir.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_USERDIR_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_userdir.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_userdir.d"; then
+        echo "Found probes for userdir $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_userdir.d $realobjects && cp probes_userdir.o .libs/probes_userdir.o && cp probes_userdir.libtool.hack probes_userdir.lo"
+        DTRACE_OBJ=probes_userdir.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_userdir.h -s $modpath_current/probes_userdir.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_userdir.so $realobjects .libs/probes_userdir.o \$(MOD_USERDIR_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_userdir.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_userdir.o .libs/probes_userdir.o && cp probes_userdir.libtool.hack probes_userdir.lo"
+        DTRACE_OBJ=probes_userdir.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_userdir.so $realobjects .libs/probes_userdir.o \$(MOD_USERDIR_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_USERDIR_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_USERDIR_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -17932,23 +21188,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_alias.libtool.hack<<EOF
+# probes_alias.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_alias.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_alias.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_alias.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_alias.d"; then
+        echo "Found probes for alias $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_alias.d $realobjects && cp probes_alias.o .libs/probes_alias.o && cp probes_alias.libtool.hack probes_alias.lo"
+        DTRACE_OBJ=probes_alias.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_alias.h -s $modpath_current/probes_alias.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_alias.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_alias.o .libs/probes_alias.o && cp probes_alias.libtool.hack probes_alias.lo"
+        DTRACE_OBJ=probes_alias.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_ALIAS_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_alias.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_alias.d"; then
+        echo "Found probes for alias $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_alias.d $realobjects && cp probes_alias.o .libs/probes_alias.o && cp probes_alias.libtool.hack probes_alias.lo"
+        DTRACE_OBJ=probes_alias.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_alias.h -s $modpath_current/probes_alias.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_alias.so $realobjects .libs/probes_alias.o \$(MOD_ALIAS_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_alias.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_alias.o .libs/probes_alias.o && cp probes_alias.libtool.hack probes_alias.lo"
+        DTRACE_OBJ=probes_alias.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_alias.so $realobjects .libs/probes_alias.o \$(MOD_ALIAS_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_ALIAS_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_ALIAS_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -18026,23 +21322,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_rewrite.libtool.hack<<EOF
+# probes_rewrite.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_rewrite.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_rewrite.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_rewrite.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_rewrite.d"; then
+        echo "Found probes for rewrite $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_rewrite.d $realobjects && cp probes_rewrite.o .libs/probes_rewrite.o && cp probes_rewrite.libtool.hack probes_rewrite.lo"
+        DTRACE_OBJ=probes_rewrite.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_rewrite.h -s $modpath_current/probes_rewrite.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_rewrite.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_rewrite.o .libs/probes_rewrite.o && cp probes_rewrite.libtool.hack probes_rewrite.lo"
+        DTRACE_OBJ=probes_rewrite.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_REWRITE_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_rewrite.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_rewrite.d"; then
+        echo "Found probes for rewrite $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_rewrite.d $realobjects && cp probes_rewrite.o .libs/probes_rewrite.o && cp probes_rewrite.libtool.hack probes_rewrite.lo"
+        DTRACE_OBJ=probes_rewrite.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_rewrite.h -s $modpath_current/probes_rewrite.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_rewrite.so $realobjects .libs/probes_rewrite.o \$(MOD_REWRITE_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_rewrite.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_rewrite.o .libs/probes_rewrite.o && cp probes_rewrite.libtool.hack probes_rewrite.lo"
+        DTRACE_OBJ=probes_rewrite.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_rewrite.so $realobjects .libs/probes_rewrite.o \$(MOD_REWRITE_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_REWRITE_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_REWRITE_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -18203,23 +21539,63 @@
   fi
 
   if test -z "$module_standalone"; then
+    cat >$modpath_current/probes_so.libtool.hack<<EOF
+# probes_so.lo - a libtool object file
+# Generated by ltmain.sh - GNU libtool 1.5.20 (1.1220.2.287 2005/08/31 18:54:15)
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# Name of the PIC object.
+pic_object='.libs/probes_so.o'
+
+# Name of the non-PIC object.
+non_pic_object='probes_so.o'
+EOF
     if test -z "$shared"; then
       # The filename of a convenience library must have a "lib" prefix:
       libname="libmod_so.la"
       BUILTIN_LIBS="$BUILTIN_LIBS $modpath_current/$libname"
       modpath_static="$modpath_static $libname"
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;'`
+      pathobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e "s#^#$modpath_current/#;" -e "s# # $modpath_current/#g;"`
+      if test -r "$modpath_current/mod_so.d"; then
+        echo "Found probes for so $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_so.d $realobjects && cp probes_so.o .libs/probes_so.o && cp probes_so.libtool.hack probes_so.lo"
+        DTRACE_OBJ=probes_so.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_so.h -s $modpath_current/probes_so.d
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_so.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_so.o .libs/probes_so.o && cp probes_so.libtool.hack probes_so.lo"
+        DTRACE_OBJ=probes_so.o
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $objects
 	\$(MOD_LINK) $objects \$(MOD_SO_LDADD)
+	ld -r -o `echo $libname | sed -e 's/\.la/.o/;'` $realobjects
+	echo "$pathobjects" > `echo $libname | sed -e 's/\.la/.objects/;'`
 EOF
     else
       apache_need_shared=yes
       libname="mod_so.la"
       shobjects=`echo $objects | sed 's/\.lo/.slo/g'`
+      realobjects=`echo $objects | sed -e 's/\.lo/.o/g;' -e 's/^/.libs\//g;' -e 's/ / .libs\//g;'`
       modpath_shared="$modpath_shared $libname"
+      if test -r "$modpath_current/probes_so.d"; then
+        echo "Found probes for so $DTRACE"
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -s \$(top_srcdir)/apache_probes.d -s probes_so.d $realobjects && cp probes_so.o .libs/probes_so.o && cp probes_so.libtool.hack probes_so.lo"
+        DTRACE_OBJ=probes_so.lo
+        $DTRACE $DTRACEFLAGS -h -o $modpath_current/probes_so.h -s $modpath_current/probes_so.d
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_so.so $realobjects .libs/probes_so.o \$(MOD_SO_LDADD)"
+      else
+	BUILD_DTRACE="\$(DTRACE) \$(DTRACEFLAGS) -G -o probes_so.o -s \$(top_srcdir)/apache_probes.d $realobjects && cp probes_so.o .libs/probes_so.o && cp probes_so.libtool.hack probes_so.lo"
+        DTRACE_OBJ=probes_so.lo
+        RELINK="\$(CC) -shared \$(CFLAGS) -o .libs/mod_so.so $realobjects .libs/probes_so.o \$(MOD_SO_LDADD)"
+      fi
       cat >>$modpath_current/modules.mk<<EOF
 $libname: $shobjects
-	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects \$(MOD_SO_LDADD)
+	$BUILD_DTRACE
+	\$(SH_LINK) -rpath \$(libexecdir) -module -avoid-version  $objects $DTRACE_OBJ \$(MOD_SO_LDADD)
+	$RELINK
 EOF
     fi
   fi
@@ -19053,6 +22429,7 @@
 HTTPD_VERSION=`$abs_srcdir/build/get-version.sh all $abs_srcdir/include/ap_release.h AP_SERVER`
 
 
+echo >>static_modules.mk
 ac_config_files="$ac_config_files $APACHE_OUTPUT_FILES docs/conf/httpd.conf docs/conf/extra/httpd-autoindex.conf docs/conf/extra/httpd-dav.conf docs/conf/extra/httpd-default.conf docs/conf/extra/httpd-info.conf docs/conf/extra/httpd-languages.conf docs/conf/extra/httpd-manual.conf docs/conf/extra/httpd-mpm.conf docs/conf/extra/httpd-multilang-errordoc.conf docs/conf/extra/httpd-ssl.conf docs/conf/extra/httpd-userdir.conf docs/conf/extra/httpd-vhosts.conf include/ap_config_layout.h support/apxs support/apachectl support/dbmmanage support/envvars-std support/log_server_status support/logresolve.pl support/phf_abuse_log.cgi support/split-logfile build/rules.mk build/pkg/pkginfo build/config_vars.sh"
 
 ac_config_commands="$ac_config_commands default"
diff -ruN httpd-2.2.16.orig/include/ap_config_auto.h.in httpd-2.2.16/include/ap_config_auto.h.in
--- httpd-2.2.16.orig/include/ap_config_auto.h.in	2010-07-22 09:07:29.000000000 +0000
+++ httpd-2.2.16/include/ap_config_auto.h.in	2011-02-23 18:38:20.249510399 +0000
@@ -6,6 +6,9 @@
 /* SuExec root directory */
 #undef AP_DOC_ROOT
 
+/* Enable DTrace probes */
+#undef AP_ENABLE_DTRACE
+
 /* Allow modules to run hook after a fatal exception */
 #undef AP_ENABLE_EXCEPTION_HOOK
 
@@ -160,6 +163,9 @@
 /* Define to 1 if you have the <sys/resource.h> header file. */
 #undef HAVE_SYS_RESOURCE_H
 
+/* Define to 1 if you have the <sys/sdt.h> header file. */
+#undef HAVE_SYS_SDT_H
+
 /* Define to 1 if you have the <sys/sem.h> header file. */
 #undef HAVE_SYS_SEM_H
 
